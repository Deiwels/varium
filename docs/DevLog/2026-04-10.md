# 2026-04-10

## Done

### Messages page — staff sync
- Fixed: fetched `/api/barbers` (barber IDs) but chat uses user IDs. Only 1 worker showed.
- Now fetches `/api/users` first, fallback `/api/barbers`. Self-filtering checks `uid` + `barber_id`.

### Payroll — default date range
- Changed default from `daysAgo(14)` to `thisWeekMonday()` — current week Mon→today.

### Payroll — show all barbers
- Backend `/api/payroll` now includes all active barbers even with 0 bookings.

### Payroll — admin section not showing
- `/api/users` returns array, payroll expected `{ users: [] }`. Fixed response parsing.

### Cash payment — "amount required" error
- Services without price caused $0 amount. Added manual amount input when `basePrice <= 0`.

### Admin schedule editor
- New component in payroll: 7-day schedule with ON/OFF + time inputs per day.
- Backend returns `schedule` from `/api/users`. Saves via `PATCH /api/users/:id`.

### Dashboard clock-in widget
- Shows for admin/barber when `clock_in_enabled` setting is true.

### Settings — attendance & geofence
- New section: enable clock-in, shop address, geocode, radius config.

## Files changed
- `app/messages/page.tsx`, `app/payroll/page.tsx`, `app/calendar/booking-modal.tsx`
- `app/dashboard/page.tsx`, `app/settings/page.tsx`, `backend/index.js`
