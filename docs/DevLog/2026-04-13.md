# 2026-04-13

> [[Home]] > DevLog | Related: [[Attendance & Payroll]], [[Booking System]], [[Payments]]

## Done

### Payroll — admin profit proportional to days worked
- Was: full profit share regardless of attendance
- Now: `profitShare = ownerShare * pct * (daysWorked / periodDays)`
- Shows "Profit 2% (3/7d): $X.XX" in UI

### Tips consistency fix
- POST /api/payments/terminal only saved `tip_amount`, not `tip` field
- Frontend PATCH sent `tip` but not `tip_amount`
- Now both fields saved together in both POST and PATCH
- Payroll reads `b.tip || b.tip_amount` — both always present now

### Cash register — field normalization
- Cash page showed $0 — backend field names mismatched frontend expectations
- `payment_method` vs `method`, `amount_cents` vs `amount`, `created_at` vs `date`
- Frontend now handles both naming conventions

### Cash total inflated fix
- `/api/payments` normalization used `amount_cents` (total with tax/fees) for local payments
- `tip_amount` (dollars) was divided by 100 as if cents
- Fix: local payments use `service_amount` (net), tips treated as dollars

### Cash page — compact layout
- Removed duplicated cash/zelle cards in day rows
- Day rows show inline amounts: "$49.99 cash · $0.00 zelle"

### Expenses system fix
- `/api/expenses` now filters by from/to date (was ignoring params)
- `/api/expenses/total` returns `by_category` breakdown
- Frontend handles array response format
- Payroll shows expense category breakdown under owner net profit

### Payroll Smart Audit system
- `GET /api/payroll/audit` — 7 cross-reference checks:
  1. Unpaid completed bookings (filters out blocks, pending, $0)
  2. Booking ↔ Payment match
  3. Cash reconciliation (expected vs counted)
  4. Admin attendance verification
  5. Period totals summary
  6. Missing service amounts
  7. Square verification (compares LINKED payments only, unlinked shown separately)
- "Audit" button in payroll topbar with expandable color-coded results panel
- Green (ok), gold (warning), red (error) with summary badges

### Automatic audit notifications
- Background job every 6 hours (throttle persisted in Firestore, survives cold starts)
- Anti-spam: hash comparison — only notifies when warnings CHANGE
- Email to owner only (Resend, dark-cosmos template, no emojis)
- Push notifications DISABLED (was too frequent/spammy)
- Red badge with `!` on Payroll icon in Dashboard (not bottom nav)
- `GET /api/payroll/audit/status` for frontend badge polling

### Audit accuracy improvements
- Unpaid bookings: skip type=block, status=booked/pending, $0 amount
- Square verification: compare only linked payments (matching payment_id)
- Unlinked Square payments (walk-ins) shown as info, not mismatch

### Cloud Run deployment fix
- **Root cause**: `SyntaxError: Identifier 'pubSettingsDocPre' has already been declared`
- Other AI added `pubSettingsDocPre` at top of route handler for online_booking_enabled check
- Same variable was declared again later for timezone — duplicate `const` in same scope
- Fix: removed duplicate declarations, reuse `pubSettingsDataPre` from earlier
- Also reverted other AI's changes: removed process.exit(1) on Firestore init, removed process.on handlers
- Increased Cloud Run resources: memory 512Mi→1Gi, added cpu 1, timeout 300s

### Settings — mobile drill-down navigation
- Old behavior: tap category, content appeared somewhere lower on the same page
- Problem: users had to guess they should scroll down to find the settings they just opened
- New mobile behavior: tap category -> open dedicated detail screen for that category
- Added `All Settings` back button to return to the category list
- Category list hides while detail screen is open on phone layout
- Desktop split-view behavior stays unchanged
- Goal: settings should feel closer to iPhone-style navigation, not like hidden expandable content

### Dark Cosmos Style Guide — full design system documented
- Scanned entire codebase: `globals.css`, `Shell.tsx`, `StyledDialog.tsx`, `layout.tsx`, `page.tsx`, `dashboard/page.tsx`, `CosmosParallax.tsx`, all components with inline styles
- Created comprehensive style reference in Claude memory (`style_varium_dark_cosmos.md`)
- Covers: typography (Inter + Julius Sans One, clamp sizes), color palette (rgba-based accent system), spacing scale, border-radius, shadows, backdrop-filter, 11 keyframe animations, component patterns (navbar, glass card, 4 button types, inputs, dialogs, chips, KPI cards, toggles, tabs), cosmic background system (starfield 3 layers, black hole orb, nebula, horizon grid, noise), responsive breakpoints, scrollbar, accessibility
- Purpose: single source of truth for Dark Cosmos style — no more reinventing styles when building new UI

### Documentation
- Created `docs/DevLog/2026-04-10.md` — all changes from April 10
- Updated `docs/DevLog/2026-04-12.md` — all changes from April 12
- Updated `docs/Features/Attendance & Payroll.md` — full documentation with formulas
- Memory saved: always update docs after changes

## Payroll calculation formulas (verified correct)

### Barber payout
```
barber_service_share = net_service_amount * effective_rate%
barber_tips = tips * tips_pct%
barber_total = barber_service_share + barber_tips
```
- net_service_amount = base price WITHOUT tax/fees (stored as `service_amount`)
- Default: 60% services, 100% tips — configurable per barber

### Admin payout
```
base_pay = hourly_rate * hours_worked
profit_share = owner_share * profit_pct * (days_worked / period_days)
fee_share = terminal_services * fee_pct (only if worked any days)
admin_total = base_pay + profit_share + fee_share
```
- Profit share proportional to days actually worked
- Default: $0/hr, 2% profit, 3% fee — configurable per admin

### Owner net profit
```
owner_share = gross_services - team_total_payout
owner_net = owner_share - admin_pay - expenses
```

### SMS & 10DLC — Telnyx campaign registration

#### Vurium Inc. brand verified on Telnyx
- Brand TCR ID: BCFAC3G, Status: Verified
- Legal entity: Vurium Inc. (Illinois corporation, EIN 99-3664614)
- displayName should be "VuriumBook" (needs confirmation from Jonathan)

#### Privacy Policy + Terms updated for carrier review compliance
- **Sender identity**: changed from "Vurium and the business" → "VuriumBook (operated by Vurium Inc.)"
- **Added to both programs**: Program Name, Message Sender, Customer Care contact, "no marketing", separate SMS consent
- **Program A (Appointment Notifications)**: strengthened no-sharing clause with TCR-required exclusion sentence
- **Program B (2FA)**: added consent-not-condition, separate consent, customer care
- **General Sharing section** (Section 5): added robust SMS opt-in data exclusion clause
- **Terms**: replaced "on behalf of the business" → "about your appointment with the business" (710 risk mitigation)
- Deployed to production: commits `7c48e37` and `f2342c0`

#### 2FA campaign submitted
- Use case: 2FA
- Brand: Vurium (BCFAC3G)
- Status: Submitted, pending carrier review (1-7 business days)
- Samples: 3 verification code messages, all starting with "VuriumBook:"

#### CUSTOMER_CARE campaign submitted and REJECTED
- Use case: Customer Care
- TCR Campaign ID: CKAOXOW
- **Status: Failed MNO Review — code 710**
- Rejection: "Reseller / Non-compliant KYC. Register the brand info, not the agency behind the brand."
- **Root cause**: Carrier saw different business names (Element Barbershop, North Shore Salon, Fade District Barbers) in sample messages and concluded VuriumBook is a reseller/agency, not the real sender
- **Next step**: Contact Jonathan (Telnyx support) for guidance on resubmission vs per-business brand architecture

#### Platform-as-sender research
- Telnyx documents "Shared campaign across customers" pattern for SaaS platforms
- Square, Booksy, Vagaro all use platform-level sender infrastructure for appointment SMS
- However, carrier reviewers may still reject if business names appear prominently in samples
- Options: (A) resubmit with adjusted wording, (B) remove business names from samples, (C) switch to per-business brands

#### New feature doc created
- `docs/Features/SMS & 10DLC.md` — full documentation of SMS architecture, campaigns, compliance requirements, rejection codes, fees, consent records

## Files changed
- `app/payroll/page.tsx` — audit UI, proportional admin profit, KPI strip
- `app/cash/page.tsx` — field normalization, compact layout
- `app/expenses/page.tsx` — response parsing fix
- `app/calendar/booking-modal.tsx` — tips consistency (both tip + tip_amount)
- `app/dashboard/page.tsx` — audit badge on Payroll icon
- `app/settings/page.tsx` — mobile drill-down settings navigation with dedicated detail screen
- `backend/index.js` — audit endpoint, periodic job, tips fix, expenses fix, cash normalization, duplicate variable fix, Firestore init revert
- `components/Shell.tsx` — removed audit badge from bottom nav
- `.github/workflows/deploy-backend.yml` — memory 1Gi, cpu 1, timeout 300s
- `docs/DevLog/` — documentation for all sessions
- `docs/Features/Attendance & Payroll.md` — full feature docs
- `docs/Features/SMS & 10DLC.md` — new: full SMS/10DLC documentation
- `app/privacy/page.tsx` — SMS sender identity, no-sharing clause, separate consent, customer care
- `app/terms/page.tsx` — SMS sender identity fix ("about" not "on behalf of"), program names, customer care
