# 2026-04-15

> [[Home]] > DevLog | Related: [[Booking System]], [[SMS & 10DLC]], [[Client Management]], [[API Routes]]

## Done

### AI 2 review landed for BE.8 + BE.9 plan gate

AI 2 completed the required frontend / browser / UX review for:

- [[BE.8-Legacy-SMS-Migration-Plan]]
- [[BE.9-DOMPurify-Custom-HTML-Plan]]

Review output is recorded in [[Tasks/BE.8-BE.9-AI2-Review]] and the 4-AI gate checklist in [[Tasks/In Progress|In Progress]] was updated to reflect that AI 2 review is now complete for both tasks.

Main findings:

- **BE.8 is not frontend-neutral** — legacy status strings are still referenced in [app/settings/page.tsx](/Users/nazarii/Downloads/varium/app/settings/page.tsx:26) and [app/developer/sms/page.tsx](/Users/nazarii/Downloads/varium/app/developer/sms/page.tsx:30), so collapsing statuses affects owner/admin UI and must be an explicit product decision in the final plan
- **BE.9 needs a stronger frontend definition** — DOMPurify is appropriate for final custom HTML output, but not a real CSS sanitizer for raw `<style>` text; the final plan must explicitly separate HTML handling from CSS handling and define how `processCustomHTML()` at [app/book/[id]/page.tsx](/Users/nazarii/Downloads/varium/app/book/%5Bid%5D/page.tsx:161) fits into the sanitization order

No product code changed in this step — this was a gate review only.

### Process hardening — AI 3 planner inbox + mandatory 4-AI plan review gate

Updated the collaboration docs so future rule changes and planning requests are harder to miss:

- added [[AI-Rule-Updates]] as the new "what changed?" file every AI must read before each session
- updated [[AI-Core-Manifesto]] so:
  - new rules must be mirrored into docs, not left in chat
  - planning requests must be written in `docs/Tasks/In Progress.md` as `@AI3 [PLAN REQUEST]`
  - AI 3 treats that queue as first priority on session start
  - complex work stays blocked until a mandatory **4-AI Plan Review Gate** is complete
- updated [[Home]], [[AI-Profiles/README]], [[AI-3-Verdent]], [[Tasks/3-AI-Remaining-Work-Split]], [[Tasks/In Progress]], and [[AI-Session-Acceptance-Log]] so the same rule is visible from every normal startup path

The intent is to stop two failure modes:

1. New cross-AI rules appearing in chat and being missed later
2. Complex work starting after only a single AI plan instead of a reviewed, improved, and jointly approved plan

### iOS app auth bootstrap hotfix — native cookie mismatch caused black screen / endless sign-in

Owner reported a critical regression in the native iOS app (the `WKWebView` wrapper around `vurium.com`):

- already logged-in users saw a black screen
- fresh sign-in inside the app could get stuck in an endless loading loop

Investigation across the web repo and the native Swift wrapper found a contract mismatch, not a rendering-only bug.

**Web expectations**

- `middleware.ts` guarded app routes using only the canonical role cookie `VURIUMBOOK_TOKEN`
- `components/Shell.tsx` expected `localStorage.VURIUMBOOK_TOKEN` to exist immediately on first paint; otherwise it redirected to `/signin`

**Native iOS reality**

In `/Users/nazarii/Desktop/untitled folder/VuriumBook/VuriumBook/VuriumWebView.swift`, cold-start boot still seeds the web session with:

- `vuriumbook_auth` = `role:uid`
- `vuriumbook_token` = bearer token

That means the native app was restoring a legacy cookie contract while the website had moved on to the canonical `VURIUMBOOK_TOKEN` contract. The result was:

1. `WKWebView` opens `/dashboard`
2. `middleware.ts` does not see `VURIUMBOOK_TOKEN` and redirects to `/signin`
3. native/user-script restore races with the web bootstrap
4. `Shell.tsx` sees no `localStorage.VURIUMBOOK_TOKEN` yet and marks the session as `noauth`
5. user gets trapped in a black-screen / signin loop

**Hotfix landed in the web repo**

- `middleware.ts`
  - accepts legacy `vuriumbook_auth` as a fallback role cookie
  - mirrors it forward into canonical `VURIUMBOOK_TOKEN` so the session self-heals
- `components/Shell.tsx`
  - restores `localStorage.VURIUMBOOK_TOKEN` from legacy `vuriumbook_token` before declaring `noauth`
- `lib/auth-cookie.ts`
  - writes both `VURIUMBOOK_TOKEN` and `vuriumbook_auth`
  - clears `VURIUMBOOK_TOKEN`, `vuriumbook_auth`, and `vuriumbook_token` on logout so native stale sessions do not resurrect themselves

**Second half of the loop (caught after the first hotfix).**

The owner then reported a more specific live symptom: the iOS app briefly showed the main screen and instantly threw the user back into loading, over and over, making the sign-in form unusable. Claude's review branch identified the missing piece:

- on `401`, `lib/api.ts` cleared `localStorage` but did **not** reliably kill the JS role cookie
- `middleware.ts` still saw that cookie and redirected `/signin` back to `/dashboard`
- Safari / WKWebView were also ignoring our cookie delete in some cases because the original cookie was set with `Secure` and the delete path did not always match attributes

Follow-up fix:

- `lib/api.ts` now calls `clearAuthCookie()` on any non-login `401`
- `lib/auth-cookie.ts` now includes the `Secure` attribute on delete when running under HTTPS, matching how the cookie was originally set
- `lib/api.ts` and `components/Shell.tsx` now invoke the native `logout` bridge on forced auth teardown, which makes `VuriumWebView.swift` clear `UserDefaults` and prevents the native wrapper from re-injecting the same expired JWT into `localStorage` on the next `/signin` load

That follow-up hotfix is what should stop the visible dashboard-flash / endless-loading loop on iOS.

**✅ CONFIRMED WORKING (2026-04-14 20:10, owner live-tested on device)**

Owner fully killed the iOS app, waited for the Vercel deploy of `c97e184` to go READY, reopened the app — the loop is gone, sign-in works end-to-end. All three hotfixes together (`95d40fc` + `59fdd7b` + `c97e184`) form the complete fix chain:

| SHA | Scope | Role in the chain |
|---|---|---|
| `95d40fc` | `middleware.ts`, `components/Shell.tsx`, `lib/auth-cookie.ts` | Accept legacy `vuriumbook_auth` cookie, mirror forward to canonical, fall back to legacy `vuriumbook_token` for bearer |
| `59fdd7b` | `lib/api.ts`, `lib/auth-cookie.ts` | On `401`, clear all three cookie aliases with the `Secure` flag (required for Safari/WKWebView to honour delete) |
| `c97e184` | `lib/api.ts`, `components/Shell.tsx` | On forced sign-out, `postMessage('logout')` into the native `webkit.messageHandlers.logout` bridge so `VuriumWebView.swift` line 203-207 clears `vurium_auth_token` and `vurium_user_json` from `UserDefaults`. Without this, Swift's cold-start restore script (line 468-488) kept re-injecting the same expired JWT on every `/signin` load. |

AI 1 cross-verification of native side (read-only — did not touch the Swift bundle):

- `/Users/nazarii/Desktop/untitled folder/VuriumBook/VuriumBook/VuriumWebView.swift:64` — `contentController.add(context.coordinator, name: "logout")` — the `logout` message handler is already registered in the shipped bundle, so `c97e184`'s `postMessage` call reaches native without needing a native rebuild
- `VuriumWebView.swift:203-207` — handler removes exactly the two UserDefaults keys (`vurium_auth_token`, `vurium_user_json`) that Swift re-injects at cold start
- `VuriumWebView.swift:468-488` — page-load restore script reads `UserDefaults` and re-pushes the token into `localStorage`; once native storage is cleared, this becomes a no-op and the loop dies

**Why this is the smallest safe fix**

- it fixes the current production incident without waiting for an App Store/native rebuild
- it is backward-compatible with existing iOS installs
- it preserves the canonical web contract instead of rolling the website back to the native app's older naming

**Native follow-up still recommended**

Claude / AI 1 should align the Swift wrapper later so the iOS app sets the canonical role cookie `VURIUMBOOK_TOKEN` directly and stops depending on `vuriumbook_auth` for route gating. The web hotfix makes the system tolerant now; it does not erase the native naming mismatch.

**Permanent prevention docs added**

To stop this class of incident from repeating during future "cleanup" work:

- added [[Web-Native-Auth-Contract]] as the permanent source of truth for web/native auth compatibility
- updated [[iOS App]] with release guardrails and verification requirements
- updated [[Auth Flow]] so it no longer falsely describes production auth as cookie-only
- explicitly gated FE.20 in [[Tasks/3-AI-Remaining-Work-Split]] so nobody removes `localStorage.VURIUMBOOK_TOKEN` support before the native wrapper is aligned

### Element 10DLC CICHCOJ — 📨 SUBMITTED TO MNO REVIEW (2026-04-14 20:30)

After the full remediation chain + VERIFIED READY flip + two-typo fix by Owner, AI 1 (Claude) filled every field in the Telnyx Edit Campaign form via Chrome MCP automation. Owner then reviewed and clicked `Save` manually (Claude does not click irreversible action buttons with a $15 carrier review fee).

**Campaign ID:** `CICHCOJ` (internal uuid `4b30019d-826e-61b2-0eb0-915fdcaf1749`)

**Status:** `Pending Telnyx Review` → MNO queue. $15 carrier fee charged.

**Key rewrites applied to the form before submit:**

- Campaign Description expanded to include `reschedule notices` (matches real product behavior)
- Opt In Workflow / Message Flow **completely rewritten** to match live checkbox render in `app/book/[id]/page.tsx:1786` — the exact quote `"I agree to receive Element Barbershop Appointment Notifications via SMS..."` is now in the Telnyx description instead of the old `"By providing your phone number..."` wording that did not match production
- Opt out keywords expanded `STOP,UNSUBSCRIBE` → `STOP,UNSUBSCRIBE,CANCEL,END,QUIT` to match `backend/index.js:1834` inbound handler
- Help keywords expanded `HELP` → `HELP,INFO`
- Sample link field filled with `https://vurium.com/book/elementbarbershop`
- Embedded Phone Number flipped `No` → `Yes` (the HELP auto-response contains `(847) 630-1884`, so strict accuracy required)
- Webhook URL + Webhook Failover URL both filled with `https://vuriumbook-api-431945333485.us-central1.run.app/api/webhooks/telnyx-10dlc`

**Why this should pass where the previous submit failed:**

| Previous MNO failure reason | How this submit addresses it |
|---|---|
| "Brand website lacking sufficient information about the company and its products" | Message Flow field now explicitly lists business name, full address with ZIP, phone, email, team members, and services. Live booking page `/book/elementbarbershop` renders all of this (verified earlier today). |
| "Call-to-action does not contain registered/DBA brand name" | Message Flow + Description contain `Element Barbershop` multiple times. CTA URL in campaign meta is `https://vurium.com/book/elementbarbershop` which MNO reviewer will click through and see brand name in page header + consent checkbox label. |

**Full final form state** is captured in [[Tasks/Element-10DLC-Resubmission-Checklist]] under the `📨 SUBMITTED — AWAITING MNO REVIEW` section with the complete field table as submit evidence.

**Post-submit monitoring:** Claude monitors `POST /api/webhooks/telnyx-10dlc` (backend line 1873) for status transitions. On `approved` → update `docs/Features/SMS & 10DLC.md` Element campaign table to Active + close In Progress section. On `rejected` → STOP, analyze rejection reason against submitted form state, remediate, and only then resubmit (every resubmit costs another $15).

### Element 10DLC CICHCOJ — VERIFIED READY for resubmit (all gates green)

After the full remediation chain that landed today, Element Barbershop is now cleared for Telnyx resubmission. Status flipped BLOCKED → READY when all four pre-resubmit gates closed:

1. **Owner fixed 2 Settings typos** (`Bufalo Grove` → `Buffalo Grove, IL 60089`; `element-barbersho.com` → `element-barbershop.com`)
2. **Codex (AI 2) ran FE.Element-Verify.1–5** live in Chrome DevTools + iPhone Safari 375 px — all 5 green
3. **AI 1 (Claude) independently re-verified** the backend `/public/config/` on production against the corrected values
4. **All 10+ remediation commits** from today are deployed: `e97efd9` backend per-workspace messageFlow URL + config business fields, `dbc8dfa` frontend Business details + Services preview, `b74c79b` consent before phone entry, `bed4537` + `8f7bec3` custom page compliance, `c2d0a99` legal-link business context, `779f524` Suspense wrapper to keep Vercel CSR bailout happy

**Live verified backend state (curl against production 2026-04-15):**

```
shop_name:    'Element Barbershop'
shop_address: '1142 W Lake Cook Rd, Buffalo Grove, IL 60089'
shop_email:   'contacts@element-barbershop.com'
shop_phone:   '+1 (224) 584-5072'
```

**Codex FE verification report:**

- Booking page shows `Verified business details` block with name / address / phone / email
- CTA reads `Book with Element Barbershop`
- SMS consent text reads `Element Barbershop Appointment Notifications` and paints on first render (the `b74c79b` fix)
- `Privacy Policy` / `Terms` links carry `business` + `slug` query params and the legal pages render the Element-branded highlight panels (the `c2d0a99` fix)
- 5-icon pill bar still correct after `71a20e2`

**MNO failure-reason mapping (closed):**

- ✅ `"The brand website is lacking sufficient information about the company and its products"` — Element public page now shows name, full address with ZIP, phone, email, 40 services with prices, 6-person team, SMS consent before submit
- ✅ `"Call-to-action does not contain registered/DBA brand name"` — CTA contains `Element Barbershop`; consent label contains `Element Barbershop Appointment Notifications`; `messageFlow` submitted via the per-workspace URL via `getWorkspaceBookingUrl()`

**Remaining owner step before click:** verify in Telnyx Portal → 10DLC → Campaigns → CICHCOJ submission form that the CTA URL field is `https://vurium.com/book/elementbarbershop` (per-workspace) and not the generic `https://vurium.com/book/` that the previous failed submission used. Our backend submits correctly on fresh submissions, but if the portal pre-populated the re-submit form from the earlier submission, the generic URL could have carried over. One-second visual check, then click Resubmit.

After the owner clicks Resubmit, the post-resubmit protocol is in `docs/Tasks/In Progress.md` → "ELEMENT 10DLC RESUBMIT — VERIFIED READY" → Post-resubmit protocol section. Next Telnyx webhook (`POST /api/webhooks/telnyx-10dlc`) will flip `sms_registration_status` on `workspaces/EZaC81SVGM0uuoYMxBCT/settings/config`.

### BE.1 — Distributed lock for background jobs (`withJobLock()`)

**Problem.** The `setInterval(() => { ... }, 3 * 60 * 1000)` block at `backend/index.js:10257` dispatched 7 background jobs every 3 minutes without any cross-instance coordination. When Cloud Run scales to multiple instances under load (cold start while warm instance is still serving, load spike, Cloud Run auto-scale), every instance fires all 7 jobs simultaneously — duplicate SMS reminders, duplicate auto-notify emails, duplicate Telnyx provision attempts, duplicate payroll audits, duplicate booking audits, double Anthropic spend on AI diagnostic scans. Currently `min_instances=0` so the risk is dormant most of the time, but it becomes real any time Cloud Run spins up a second warm instance.

**Fix.** New `withJobLock(jobName, ttlSeconds, fn)` helper uses Firestore top-level `job_locks/{jobName}` collection as a distributed TTL lock. Each Cloud Run instance generates a `JOB_INSTANCE_ID` (`crypto.randomBytes(8).toString('hex')`) once at module load. Before running a job, the helper opens a Firestore transaction that:
1. Reads the lock doc
2. If it exists with `locked_until > now` AND `locked_by !== our instance id` → skips this cycle
3. Otherwise → writes the lock with our instance id and `locked_until = now + ttlSeconds`

After the job completes, a second transaction deletes the lock *only if we still own it* (guards against stomping on a replacement holder that grabbed the lock after TTL). If the release transaction fails, the lock expires naturally via TTL (within at most 15 minutes for the longest-running jobs).

**Jobs wrapped** (at `backend/index.js:10342`):

| Job | TTL |
|---|---|
| `runAutoReminders` | 600 s |
| `runAutoMemberships` | 600 s |
| `runRetentionCleanup` | 900 s |
| `runPayrollAudit` | 900 s |
| `runBookingAudit` | 900 s |
| `runSmsAutoProvisionRetry` | 600 s |
| `runAIDiagnosticScan` (separate interval) | 1800 s |

`resetSecurityCounters` is intentionally **not** locked — it is synchronous, in-memory only, and a double-run is harmless. Locking it would cost a Firestore round-trip for no benefit.

**What does NOT change.** Internal per-job throttles (`_lastReminderRun < 3 * 60 * 1000`, `_lastMembershipRun < 5 * 60 * 1000`, etc.) stay in place as the inner guard. The distributed lock is an **additional outer** guard for multi-instance safety, not a replacement. Job function bodies, setInterval cadence, and single-instance behavior are all functionally unchanged. When only one instance is running (99% of the time right now), the lock always acquires on first try and the jobs run exactly as before.

**What is new.**

- Const `JOB_INSTANCE_ID` at `backend/index.js:33` — unique 16-char hex per Cloud Run revision/instance
- `withJobLock()` helper at `backend/index.js:10283` — non-throwing transactional acquire + release
- Module load prints `[JOBS] Instance id: <hex> · distributed lock active` on first lock acquire so Cloud Run logs show the feature is live
- New Firestore collection `job_locks/` — at most 7 docs at any given time, written ~7 × 4 = 28 ops per 3-minute cycle = ~13k ops/day per instance. Well within free tier baseline

**Rollback.** If anything breaks, revert the commit. Existing jobs go back to unlocked `setInterval` (current behavior). No data migration, no state to clean up except leftover `job_locks/` docs which expire on their own within 30 minutes of the revert.

**Ownership + Manifesto note.** This is the first AI 1 code change after [[AI-Core-Manifesto]] landed. Rule 5 normally requires an AI 3 (Verdent) plan before architectural work. This change is a gray-zone case: one file, additive only, no logic changes to existing jobs, no data migration, no schema changes outside a new collection. Owner greenlit parallel execution to FE.Element-Verify, Codex (AI 2) explicitly confirmed "не заважає і не конфліктує" with his Element verification pass, and the inline plan is written in `docs/Tasks/In Progress.md` → "BE.1 — Distributed lock..." section before the code landed. AI 3 (Verdent) is asked to post-commit review the helper + 7 wrapped call sites and record the result in `docs/Tasks/QA-Scan-2026-04-15.md`.

**Verification after Cloud Run deploy.**

- [ ] Cloud Run logs show `[JOBS] Instance id: <hex> · distributed lock active` within the first 3 minutes of deploy
- [ ] Firestore Console → `job_locks` collection — docs appear during each 3-minute cycle and disappear after jobs finish
- [ ] `runAutoReminders`, `runAutoMemberships`, etc. still execute exactly once per cycle on a single-instance deployment (check `sms_logs` volume, compare before/after)
- [ ] Double-instance test (manual, optional): `gcloud run services update vuriumbook-api --min-instances=2` for 10 minutes, then check that `sms_logs` volume is unchanged — only one instance should emit per cycle
- [ ] Rollback test (manual, optional): kill Cloud Run instance mid-job, confirm lock expires within TTL and the next instance's cycle acquires successfully

**Files changed:** `backend/index.js` only (+102 / -9), `docs/Tasks/In Progress.md` (BE.1 plan block), `docs/DevLog/2026-04-15.md` (this entry).

### AI 2 build hotfix — `/privacy` and `/terms` searchParams CSR bailout

- Vercel build for commit `8a75770` failed during prerender of `/terms`
- Root cause: `app/privacy/page.tsx` and `app/terms/page.tsx` were converted into `'use client'` pages just to read `useSearchParams()`, which triggers a CSR bailout requirement in Next.js 15 unless wrapped in `Suspense`
- Fix applied in AI 2 scope:
  - removed `'use client'` from both legal pages
  - switched them to server-friendly `searchParams` page props
  - kept the branded `Element Barbershop via VuriumBook` legal-context panels intact
- Result: the business-aware legal-link flow remains, but `/privacy` and `/terms` can prerender safely again

### Element Barbershop pre-resubmission live verification — 2 content typos are hard blockers

Owner asked whether Element is ready for a 10DLC campaign CICHCOJ resubmission after the hotfix chain that landed throughout 2026-04-15 (`e97efd9` backend remediation, `dbc8dfa` + `b74c79b` + `bed4537` + `8f7bec3` frontend business proof, `c2d0a99` legal-link context). Before any Resubmit click, AI 1 ran a live verification against production.

**Backend content verified via `/public/config/EZaC81SVGM0uuoYMxBCT`:**

| Field | Live value (from production) | Status |
|---|---|---|
| `shop_name` | `Element Barbershop` | ✅ exact DBA match |
| `shop_address` | `1142 W Lake Cook Rd, Bufalo Grove, IL` | ❌ **typo** "Bufalo" (should be "Buffalo") + **no ZIP** (should be `60089`) |
| `shop_phone` | `+1 (224) 584-5072` | ✅ |
| `shop_email` | `contacts@element-barbersho.com` | ❌ **typo** missing "p" — should be `element-barbershop.com` |
| `business_type` | `Barbershop` | ✅ |
| `sms_brand_name` | `Element Barbershop` | ✅ DBA match |
| `online_booking_enabled` | `true` | ✅ |
| `waitlist_enabled` | omitted from response (default on after `a3c885f` hotfix) | ✅ |
| Services | 40 real services loaded via `/public/services/` — names + real prices ($24.99 – $100) + durations | ✅ strong business proof |
| Team | 6 barbers (Arsen, Dan, Lili, Naz, Vio) via `/public/barbers/` | ✅ |

**Why the typos are hard blockers for MNO resubmission.** The MNO review failure reason #1 was "brand website is lacking sufficient information about the company and its products." MNO reviewers routinely cross-check the visible business info by searching Google / WHOIS / public records for the address and email domain. With `Bufalo Grove` (wrong spelling) and `element-barbersho.com` (domain that does not resolve), both cross-checks fail. The reviewer would mark this as the same failure class we already failed. These two fields must be fixed in Element Settings before any resubmit attempt.

**Metadata / SEO verified from raw HTML of `/book/elementbarbershop` (Next.js shell + og tags):**
- `og:title = "Book with Element Barbershop"` ✅
- `og:description`, `og:site_name`, `og:image`, `canonical` — all present with Element-branded content ✅

**Frontend hydration verified via git, NOT via live curl:**
- `app/book/[id]/page.tsx` contains the Business details block (address/phone/email), Services preview grid, team grid, SMS consent copy-first rendering, Privacy/Terms links with `business` + `slug` query params — all landed between `dbc8dfa`, `b74c79b`, `bed4537`, `8f7bec3`, and `c2d0a99`
- `app/privacy/page.tsx` and `app/terms/page.tsx` read those query params via `useSearchParams` and render a business-context highlight panel above the `#sms` section

**What curl cannot verify.** Both `/book/[id]` and the legal pages are `'use client'` Next.js routes. The raw HTML returned by a non-browser HTTP client is the Next.js shell plus metadata — the React components do not render until JS hydrates in a real browser. So curl confirmed the pages return 200, Vercel is serving them, and metadata is business-branded, but could NOT confirm that the Business details block, Services preview, team grid, or legal context panel actually appear on screen. **Only a real browser visit can confirm hydration output.**

**Verdict: NOT READY for resubmission.** Two blockers:
1. Fix `shop_address` → `1142 W Lake Cook Rd, Buffalo Grove, IL 60089` (owner task)
2. Fix `shop_email` → `contacts@element-barbershop.com` (owner task)

Then:
3. Codex (AI 2) runs live browser verification pass on `/book/elementbarbershop` + `/privacy?business=Element Barbershop&slug=elementbarbershop` + `/terms?...` — confirms hydrated render matches the code intent (see `docs/Tasks/Element-10DLC-Resubmission-Checklist.md` for the exact visual checks)
4. Owner runs the same checks on a real iPhone (375 px) + desktop
5. Only after all four steps are green — click Resubmit in Telnyx portal for campaign CICHCOJ

The remediation code itself is correct. The backend data is wrong on two fields, and live hydration needs Codex eyes before we trust that every section renders.

### Element 10DLC legal-link context fix

- Booking-page `Privacy Policy` / `Terms` links no longer send reviewers to an unscoped generic SMS legal view
- `app/book/[id]/page.tsx` now appends business context (`business`, `slug`) to the legal URLs it renders and stores in the SMS consent text
- `app/privacy/page.tsx` and `app/terms/page.tsx` now read that context and show a reviewer-friendly explanation at the top of the page and directly above the `#sms` section:
  - business = `Element Barbershop` (or whichever workspace initiated the link)
  - VuriumBook = platform / infrastructure provider
  - booking / consent = tied to that specific business
- This directly addresses the owner concern that a reviewer could click from the Element booking page into a generic Vurium policy page and lose the sender/business relationship
- Result: the booking page, CTA, consent copy, and legal pages now tell the same story end-to-end: clients are booking with the business, and VuriumBook powers the booking and messaging infrastructure behind it

### Hotfix: Mobile bottom pill bar showed 8 icons instead of 5 — PERM-001 overshoot regression

**Symptom.** Owner sent a screenshot from the live waitlist page on mobile — the bottom pill navigation was rendering **8 icons** (dashboard, calendar, history, messages, waitlist, portfolio, clients, payments) for an owner/admin account. The design target per CHANGELOG 2026-04-13 is a **fixed 5-item pill**: Home · History · Calendar · Messages · Settings. Settings icon was not even visible in the screenshot because the extra items pushed everything around.

**Root cause.** Codex's commit `074ddd2 fix(frontend): align role permissions across shell and payments` fixed the original PERM-001 bug by swapping the hardcoded 5-item pill render for `{visibleNav.map(...)}`. That correctly made Shell's `visibleNav` mean something again (it was computed but never rendered before), but it overshot the fix — `visibleNav` is the full role-filtered list (up to 16 items: dashboard, calendar, history, messages, waitlist, portfolio, clients, payments, attendance, cash, membership, analytics, expenses, payroll, billing, settings). For owner accounts almost all of those pass the filter, so the pill bar became a scroll-strip of 8+ icons and the mobile design broke.

The navigation architecture is actually **two-tier**:

1. **Bottom pill bar** — 5 most-common destinations. Fixed. Always the same 5 shapes.
2. **Dashboard shortcuts grid** — every other page (Payments, Clients, Waitlist, Portfolio, Membership, Analytics, Cash, Expenses, etc.). Role + permission gated. This is where PERM-001's "all visible pages must be reachable" requirement actually lives — and Codex already made it respect `hasPerm()` in `app/dashboard/page.tsx` as part of PERM-002.

So the pill bar does NOT need to render the full `visibleNav` — the Dashboard shortcut grid does that job. The pill bar should always be the canonical 5.

**Fix** (commit hash TBD when I push). Restored hardcoded 5-item pill via a small `as const` tuple `['dashboard', 'history', 'calendar', 'messages', 'settings']`. Each id is looked up in `visibleNav.find(...)` before rendering, so:

- if a role cannot see (e.g.) messages or history, that pill slot simply does not render — matches the pre-`074ddd2` behavior exactly
- if everything is visible, pill bar always shows the same 5 icons
- the full `visibleNav` is still computed (needed for other Shell code paths and left-side nav if it ever renders)

PERM-001 remains fixed because Dashboard shortcuts + Settings sidebar cover the other pages. The visual regression is gone.

**Ownership note.** `components/Shell.tsx` is Codex (AI 2) scope per `docs/AI-Work-Split.md` and `docs/Tasks/3-AI-Remaining-Work-Split.md`. AI 1 (Claude) patched it directly on explicit owner instruction because the regression was visible on live production and the fix needed to land fast. Going forward, further Shell.tsx edits route back to Codex. This is the second ownership-rule exception today — first was Verdent touching `backend/index.js` for Element remediation. Both were absorbed into the right owner's commit queue, but the pattern is: **emergency hotfix wins over ownership split when the owner explicitly asks, but is recorded in DevLog so the pattern does not repeat silently.**

**Local preview caveat.** This machine has no `npm` / Node installed, so `preview_start` could not spin up a dev server for in-browser verification. The fix is a minimal TypeScript-safe patch (tuple of literal ids, `visibleNav.find` lookup, `return null` on miss) and should compile under the repo's `strict: false` setup without issue. Real verification happens on the next Vercel deploy — owner should visually confirm by opening `/waitlist` or any other page on mobile and checking that the pill bar shows exactly 5 icons again.

**Re-verification required.**
- [ ] After Vercel deploy lands, open `/dashboard`, `/waitlist`, `/calendar`, `/messages` on iPhone. Pill bar shows **Home · History · Calendar · Messages · Settings** only.
- [ ] Owner role: all 5 icons visible.
- [ ] Barber / student role: icons that are not visible for that role simply drop out (fewer than 5 is OK).
- [ ] Dashboard shortcuts grid still shows Payments / Clients / Waitlist / Portfolio / Membership / Analytics (PERM-002 Codex fix) — unaffected by this change.
- [ ] No horizontal scroll in pill bar on 375px width.

**Lesson learned.** When fixing "feature X is unreachable from nav", the fix belongs where the nav was intended to surface that destination — not in the first render slot that happens to import the filtered list. Bottom pill = 5 canonical tabs; side drawer or dashboard grid = everything else. The QA scan note for PERM-001 was technically correct ("visibleNav is computed but never rendered") but the correct destination for `visibleNav.map` is the sidebar drawer (which `sidebarOpen` state already half-implements in Shell), not the pill bar.

### Permission docs sync after `71a20e2`

- `docs/Tasks/In Progress.md` was refreshed so the older "AI 2 local permission batch — pending commit / verification" note no longer contradicts live `main`
- Current documented state is now:
  - `074ddd2` landed the core AI 2 permission fixes for Dashboard / Payments / profile-password access
  - `71a20e2` intentionally narrowed the Shell pill bar back to the canonical 5-item architecture
  - broader page reachability remains via Dashboard shortcuts, not via a scrolling bottom pill

### AI 2 build hotfix — `components/Shell.tsx` tab typing regression

- Vercel build for commit `b74c79b` failed on `components/Shell.tsx:330`
- Root cause: the profile-tab array widened `t.id` to `string`, so `setTab(t.id)` no longer satisfied the local state union type `'profile' | 'password'`
- Fix applied in AI 2 scope:
  - introduced a typed `profileTabs` array with `id: 'profile' | 'password'`
  - mapped the buttons from that typed array instead of the inline inferred tuple
- No behavior change; this is a TypeScript-only build fix for the profile modal tab switcher

### AI 2 build hotfix — `app/book/[id]/page.tsx` custom proof ordering regression

- Vercel build for commit `bed4537` failed on `app/book/[id]/page.tsx:830`
- Root cause: `showCustomBusinessProof` was computed before `activeTemplate` was declared, so the new custom-page proof block hit a block-scoping error during production type-checking
- Fix applied in AI 2 scope:
  - moved `showCustomBusinessProof` below template resolution, right after `activeTemplate` is computed
  - kept the Element custom-page proof behavior unchanged; this is only an ordering / build fix

### Hotfix: Waitlist disappeared from public booking page — SET-006 regression from 849e998 (commit `a3c885f`)

**Symptom:** Owner reported that the `Join waitlist` CTA on the public booking page (`/book/{slug}`) stopped rendering. It was there earlier in the week and then silently vanished after 2026-04-15 backend pushes.

**Root cause** — my own earlier SET-006 fix in commit `849e998`:
- In `/public/config/:workspace_id` I started returning `waitlist_enabled: !!data.waitlist_enabled`. The `!!` coerced *undefined* → *false*.
- In 99% of live Salon+ workspaces the `waitlist_enabled` field was **never explicitly set** in Firestore — the `Settings → Waitlist enabled` toggle defaulted to unchecked in the UI but no one had touched it. So every Salon+ workspace suddenly got `waitlist_enabled: false` from the public config endpoint.
- Frontend at `app/book/[id]/page.tsx:442` uses `setWaitlistEnabled(cfg?.waitlist_enabled ?? !!resolved.waitlist_enabled)`. The nullish coalescing `??` only falls through on `null` / `undefined`, not on `false`. So the explicit `false` from config overrode the plan-feature `true` from `/public/resolve`, and the form disappeared everywhere.

**Two other silent bugs uncovered** during the investigation, both already dead before my 849e998 fix — my change just made one of them visible:
- `POST /public/waitlist/:workspace_id` at line ~9400 rejected submissions with `403 Waitlist is currently disabled` whenever `settingsData.waitlist_enabled` was falsy, including undefined. So even before 849e998, every "click Submit" on an untouched-toggle workspace failed with 403. Form was visible but non-functional. Predates my work.
- `tryWaitlistAutoFill()` at line ~810 bailed immediately on every cancellation if `!settings.waitlist_enabled`. The auto-notify feature (cancel slot → tell waitlist) was effectively dead for every Salon+ workspace that never touched the toggle. Also predates my work.

**Fix applied in `a3c885f`** — one consistent semantic across all three sites: *waitlist is ON by default on any plan that includes the feature; the Settings toggle is an explicit OFF override, not an opt-in.*

1. **`/public/config/:workspace_id`** — only emits `waitlist_enabled` when it is an explicit boolean in Firestore. When absent, the field is omitted from the response, and the frontend `??` guard correctly falls through to the plan-feature value from `/public/resolve`.
2. **`POST /public/waitlist/:workspace_id`** — rejects only when the plan does not include waitlist OR when the owner explicitly set `waitlist_enabled === false`. Undefined (default) passes.
3. **`tryWaitlistAutoFill()`** — bails only on explicit `false`. Plan-level gate is already enforced in the caller path.

**Behavior matrix after the fix:**

| Scenario | Before `a3c885f` | After `a3c885f` |
|---|---|---|
| Salon+ workspace, toggle untouched (default) | Form hidden (regression from 849e998), submit would 403 anyway | ✅ Form visible, submit accepted |
| Salon+ workspace, explicit `waitlist_enabled: true` | Form visible, submit accepted | ✅ Same — unchanged |
| Salon+ workspace, explicit `waitlist_enabled: false` | Form hidden, submit rejected | ✅ Same — explicit off respected |
| Individual plan | Plan gate blocks regardless | ✅ Same — plan gate unchanged |
| Element Barbershop | Not affected by this path | ✅ Not affected |

**Re-verification required** after `a3c885f` deploy lands on Cloud Run — owner and Codex need to run together:

- [ ] Open `/book/{salon-workspace-slug}` in incognito → pick a fully booked day → `Join waitlist` CTA visible
- [ ] Fill waitlist form → submit → **no 403 error** → success state
- [ ] Admin `/waitlist` page shows the new entry
- [ ] Cancel an existing booking → check `workspaces/{wsId}/waitlist` for matching entries → SMS + email auto-notify fires (or email-only fallback if SMS sender is not active)
- [ ] Salon+ workspace with explicit `waitlist_enabled: false` in Firestore → form still hidden, POST still returns 403
- [ ] Individual plan workspace → form hidden (plan gate)
- [ ] Element Barbershop public page (`/book/elementbarbershop`) → Business details + Services preview still render (e97efd9 functionality unaffected)
- [ ] Run full `docs/Tasks/Element-10DLC-Resubmission-Checklist.md` top to bottom on Element

**Lesson learned.** Boolean defaults in public API responses are load-bearing. When converting an "omitted" field to an "explicit boolean" field, check every frontend consumer for `??` / `||` fallback patterns. The fix should either keep the field optional (omit when unset) OR explicitly think through what "unset" should mean and document the default. Both pre-existing backend call sites (`POST /public/waitlist/` and `tryWaitlistAutoFill`) were already making the wrong assumption — the feature "worked" before only because the frontend form was visible and users happened not to try submit on unset workspaces. My 849e998 change exposed the full chain. Treat this as a reminder to audit **all** consumers when flipping a default, not just the direct caller.

### AI 2 local permission polish batch

- Re-read [[Tasks/3-AI-Remaining-Work-Split]] and [[Tasks/In Progress]] before touching the frontend permission items, to stay inside the agreed AI 2 scope and avoid stepping on Claude's backend ownership
- `components/Shell.tsx`
  - switched the bottom pill nav from a fixed 5-item list to the computed `visibleNav`, so the real role-visible pages are navigable from the shell
  - made the nav horizontally scrollable on small screens instead of silently hiding extra destinations
  - added missing icon cases for `portfolio`, `cash`, and `billing`
  - widened `hasAnySettingsAccess` to include `change_password` / `view_pin`, not just the main settings categories
  - fixed the unreachable password flow in `ProfileModal`:
    - `Password` tab now renders when the user has `settings_access.change_password`
    - password validation copy now matches backend min-8 requirements
- `app/dashboard/page.tsx`
  - replaced the hardcoded barber/student shortcut exclusions with permission-driven filtering (`pages.*` + `settings_access`)
  - result: if a non-owner role is granted `Clients`, `Payments`, `Cash`, `Membership`, etc., the shortcut can now actually appear on the dashboard
- `app/payments/page.tsx`
  - added `usePermissions()` and a clean access-restricted state when `pages.payments` is disabled
  - removed the old `isOwner`-only frontend gating pattern
  - action visibility now tracks the backend more honestly:
    - `Reconcile` for owner/admin
    - `Sync Tips` for owner
    - `Refund` for owner/admin
- Status: local AI 2 patch only for now; still pending commit/push and browser verification after one more diff sanity pass

### Element Barbershop manual 10DLC review — new MNO failure details

- The owner received the review result for Element campaign `CICHCOJ`
- New status: **Failed MNO Review**
- Failure reasons from the reviewer:
  - `The brand website is lacking sufficient information about the company and its products.`
  - `Call-to-action does not contain registered/DBA brand name.`
- This confirms Element's blocker is now **website / CTA verification**, not the earlier platform-level 710 reseller issue
- Important implementation note for the team: current 10DLC submission code in `backend/index.js` still uses a generic `https://vurium.com/book/` path in `messageFlow`; the next safe fix is to align the submission with the exact workspace URL and exact DBA text before any resubmission
- Website-side remediation started immediately:
  - `/public/config/:workspace_id` now allowlists `shop_address`, `shop_phone`, and `shop_email`
  - `app/book/[id]/page.tsx` now renders a visible `Business details` section and `Services` preview on the landing page so MNO reviewers can see that the business has a real identity and clear offered services before the booking form
- Backend submission fidelity was also patched:
  - 10DLC `messageFlow` now uses the exact workspace booking URL instead of generic `/book/`
  - CTA wording in submission now explicitly references `${shopName} Appointment Notifications via SMS`
- AI 2 follow-up patch on `app/book/[id]/page.tsx`:
  - SMS consent copy on both booking and waitlist forms now renders immediately, before any phone input is entered
  - the checkbox remains disabled until the phone number is valid, but the reviewer can now see the full consent language, Terms, and Privacy links on first paint
  - `Business details` still depend on real saved `shop_address` / `shop_phone` / `shop_email` values from Element Settings; empty Firestore fields will still produce an empty public proof block
- Additional AI 2 compliance follow-up for custom pages:
  - when a booking page uses `custom` HTML, Vurium now still renders a standardized public proof block underneath the custom content
  - the block shows the business name, saved contact details, services preview, compliance footer text, public Privacy / Terms links, and a branded CTA (`Book with {Business}`)
  - this prevents custom template styling from hiding the exact business identity and CTA evidence the MNO reviewer is trying to verify
- Additional AI 2 cleanup after live owner feedback:
  - `site_config.sections_enabled` is now actually respected by the public booking page for hero / about / services / team / reviews
  - this specifically fixes the case where the owner hid `Services` in Settings but the public page still showed a services preview anyway
  - on custom pages, the standardized proof block now also respects the `Services` toggle instead of re-introducing the section from below
  - the custom-page footer was simplified so the SMS compliance text + Privacy / Terms links do not duplicate at the very bottom when they are already shown inside the proof block
- New external research on US A2P CTA / brand verification was distilled into docs:
  - per-business fidelity
  - exact-match legal / DBA / address discipline
  - no-login public business proof
  - multiple opt-in methods must be described in `messageFlow` if active
  - policy URLs should be treated as first-class submission artifacts
- Added: [[Tasks/US-A2P-CTA-Brand-Verification-Notes]]
- This still does **not** fully guarantee Element approval on the next attempt; the public Element page content and business proof still need a final human sanity-check before resubmission

### Temporary reminder delivery options — distilled into docs

- Reviewed new research on temporary alternatives while 10DLC is blocked by CTA / brand verification issues
- Captured the practical takeaway for the current Vurium stack in [[Tasks/Temporary-Reminder-Delivery-Options]]:
  - immediate fallback = transactional email
  - best short-term SMS alternative = per-business verified toll-free
  - hosted screenshots can be used as supplemental reviewer evidence if the live flow still reads ambiguously
  - not worth pursuing as a launch unblocker right now:
    - shared short codes
    - platform-wide shared sender
    - WhatsApp / RCS / voice
- This keeps the new research aligned with the existing April 2026 consensus:
  - Element stays on per-business remediation
  - new workspaces stay on toll-free-first
  - email remains the safe fallback when SMS is unavailable

### Telnyx Hardening — Backend Gaps 2, 3, 4, 5 (backend/index.js)

Implemented the full safe-hardening pack from `docs/Tasks/Telnyx-Integration-Plan.md`. Sender architecture unchanged — still per-workspace toll-free + grandfathered manual 10DLC + Element protected. The product change is purely in owner experience (auto-provisioning removes the "Enable SMS" friction) plus backend security/perf.

**Gap 2 — Ed25519 webhook signature verification**
- New helper `verifyTelnyxWebhookSignature(req)` — mirrors the Stripe/Square pattern already in the codebase
- No-op when `TELNYX_WEBHOOK_PUBLIC_KEY` env var is absent (dev/staging-safe; does not block before the secret is rotated in)
- Called on entry of both `POST /api/webhooks/telnyx` (inbound SMS) and `POST /api/webhooks/telnyx-10dlc` (brand/campaign status updates)
- Uses `crypto.verify(null, ...)` with Ed25519 SPKI key from base64 env var
- On failure: throws `{ statusCode: 401 }` which short-circuits webhook processing before any opt-out / reminder writes happen
- Operational gate: `TELNYX_WEBHOOK_PUBLIC_KEY` is now owner-confirmed in GitHub Secrets; the remaining step is confirming a deployed backend revision is actually enforcing webhook verification in production

**Gap 3 — `phone_number_index` for O(1) inbound webhook lookup**
- New top-level Firestore collection `phone_number_index/{e164Digits}` with fields `workspace_id`, `shop_name`, `from_number`, `number_type`, `updated_at`
- Written from both provisioning paths: `provisionTollFreeSmsForWorkspace()` after successful toll-free provision, and `POST /api/sms/verify-otp` after successful manual 10DLC SP campaign activation
- `POST /api/webhooks/telnyx` now resolves the inbound TO number via a single `phone_number_index` doc read (was: full workspace scan with N settings reads), and propagates STOP opt-out via `collectionGroup('clients').where('phone_norm', '==', ...)` instead of iterating every workspace. Pending `sms_reminders` cancellation is set-based on affected workspaces only
- Removes the hard `limit(100)` workspace cap and makes STOP/HELP latency independent of total workspace count

**Gap 4 — `runAutoReminders()` pagination**
- Replaced `db.collection('workspaces').limit(100).get()` with a `startAfter(lastDoc)` pagination loop ordered by `__name__`, batch size 50, no upper cap per cycle. Throttle (3 min) stays in place via `_lastReminderRun`

**Gap 5 — SMS auto-provisioning on plan activation**
- New non-throwing helper `autoProvisionSmsOnActivation(workspaceLike, source)` with 4 guards: legacy/protected (isLegacyManualSmsPath), already-active, provisioning-in-flight, and failed_max_retries. Only proceeds from statuses `none`, `rejected`, `failed`
- On success: clears retry metadata and records `sms_auto_provision_succeeded_at`
- On failure: increments `sms_auto_provision_retry_count`, stores `sms_auto_provision_last_error`, schedules `sms_auto_provision_next_retry_at` with exponential backoff (5 min → 15 min → 45 min → 2 h → 6 h). After 5 attempts: sets `sms_registration_status: 'failed_max_retries'`. Writes audit log entries `sms.auto_provision_failed` / `sms.auto_provision_exhausted` per attempt. Never throws — fire-and-forget callers
- Wired into three activation points: `POST /auth/signup` (source `signup_trial`), `handleStripeEvent()` for `checkout.session.completed` / `customer.subscription.created|updated` / `invoice.payment_succeeded`, and `POST /api/billing/apple-verify` (source `apple_paid`)
- New background job `runSmsAutoProvisionRetry()` wired into the main `setInterval` (3 min tick). Throttled to 5 min. Paginated workspace iteration (same pattern as Gap 4) with safety cap of 500 workspaces per cycle. Deliberately avoided `collectionGroup('settings')` to not require an extra Firestore index exemption. Fires `autoProvisionSmsOnActivation(..., 'auto_retry')` for any workspace whose `sms_auto_provision_next_retry_at` is due and status is not already terminal

**What is still NOT in code (waiting on owner / external)**
- `TELNYX_WEBHOOK_PUBLIC_KEY` GitHub Secret — owner has now confirmed the secret exists; Gap 2 now waits on deploy verification rather than secret creation
- `TELNYX_VERIFY_PROFILE_ID` still blocked on Telnyx `whitelisted_destinations` account-level issue (pending Jonathan call)
- Full platform-sender pivot — intentionally NOT implemented; see `docs/Tasks/Platform-Sender-Pivot-Decision.md`

**Collaboration note**
This backend pack was produced by a three-AI cooperative pass in the working tree (Claude + Verdent). Per `AI-Work-Split.md` and the 3-AI execution split agreed in this session, Claude (AI 1) owns `backend/index.js` and did the final review and commit. Codex (AI 2) is handling the parallel frontend SMS UX reframing in `app/settings/page.tsx` + `app/signup/page.tsx` in a separate commit. Verdent's role going forward is review / docs sanity / verification, not a third editor on owned files.

### Platform Sender Pivot Decision — Three-AI Review

- Власник запропонував перейти на Booksy/Square-модель: один Vurium-owned toll-free номер для всіх workspace (пропозиція від Verdent)
- Провели three-way review: Claude (AI 1) · Codex (AI 2) · Verdent
- **Consensus:** не робимо pivot зараз. Причини:
  - Ми вже пробували platform-as-sender у квітні 2026 і отримали **carrier code 710 rejection** ("Reseller / Non-compliant KYC")
  - Sample messages у Verdent-плані буквально ідентичні тим, що carrier відкинув
  - TFV (toll-free verification) — окрема operational робота, не code patch
  - CTIA opt-in non-transferability вимагає повністю переписаного consent flow
  - STOP isolation ламається на shared sender
- **Launch path залишається:** dual-path (per-workspace toll-free + grandfathered manual 10DLC)
- **UX покращення:** auto-provision toll-free при активації платного плану — прибирає єдиний friction point ("один клік в Settings") без зміни sender architecture
- **Parallel track:** draft листа Jonathan/Telnyx з конкретними 5 запитаннями щодо чи взагалі можливий compliant shared-sender patten для ISV як Vurium
- Написано/оновлено:
  - `docs/Tasks/Platform-Sender-Pivot-Decision.md` (новий) — full decision note з історією 710, 6 compliance obstacles, 3 варіантами, draft Jonathan letter, decision tree
  - `docs/Tasks/Telnyx-Integration-Plan.md` — додано scope section (safe hardening) + Gap 5 (auto-provision on plan activation)
  - `docs/Tasks/SMS-Strategy-Review.md` — додано pointer на decision doc
  - `docs/Tasks/In Progress.md` — нова "Decision log" + "Telnyx hardening" + "Jonathan operational track" секції
  - `docs/Home.md` — додано посилання на Platform-Sender-Pivot-Decision

### Support Emails Restyled As Real Support Correspondence
- Re-read `docs/Features/Email System.md`, `docs/Features/Developer Panel.md`, and the current email routes before changing support-email output
- Rebuilt the shared support email template in `backend/index.js` so support-facing messages now read like professional correspondence instead of a booking/reminder card
- The new template is:
  - mailbox-aware (`Support`, `Billing`, `Sales`, `Security`)
  - left-aligned and more human / team-written in tone
  - explicit about who the message is from and how to continue the conversation
- Routed the template through all relevant support-ish email paths:
  - `POST /api/vurium-dev/email/send`
  - `POST /api/vurium-dev/gmail/send`
  - `POST /api/vurium-dev/gmail/reply`
  - admin inbox-forward notifications from `POST /api/vurium-dev/email/inbound`
- Developer Panel email UI copy in `app/developer/email/page.tsx` was aligned too:
  - `Branded` wording replaced with team/support correspondence wording
  - Resend sends now pass mailbox context so the right support team label is used
- Follow-up polish:
  - replaced the baked-background logo tile with a transparent dark Vurium mark asset (`logo-transparent-dark.png`)
  - generated transparent light/dark logo assets for future email/UI use:
    - `public/logo-transparent-dark.png`
    - `public/logo-transparent-light.png`
  - removed the extra logo wrapper box so the mark sits visually centered and does not look offset inside the header
  - corrected the dark email mark asset so only the background is transparent while the logo itself stays solid and readable on dark email surfaces
  - versioned the support-email logo URL (`?v=2`) to break Gmail image cache after the asset fix
- Result: support-originated emails should now feel like they came from the Vurium team, not from the generic transactional booking template

### Messaging Docs Clarified In Home
- Re-read the newly added `docs/Messaging-System-AI1.md` and `docs/Messaging-System-AI2.md`
- Confirmed the new docs describe the messaging stack more broadly than just internal DMs:
  - in-app staff chat
  - SMS delivery touchpoints
  - transactional email touchpoints
  - push notification touchpoints
- Updated `docs/Home.md` so the `Messaging` feature row no longer undersells that system as only "Team DMs between staff"
- This keeps `Home` aligned with the new messaging reference docs for both AI 1 and AI 2

### AI2 Planning Docs Surfaced In Home
- Re-read the remaining AI2 / shared planning docs before packaging them:
  - `docs/Registration-Improvement-AI2.md`
  - `docs/Theme-Light-AI2.md`
  - `docs/PublicSite-AI2.md`
  - `docs/Home.md`
- Updated `docs/Home.md` so the documentation map now explicitly exposes both Public Mini-Site planning docs:
  - `PublicSite-AI1`
  - `PublicSite-AI2`
- Also updated the Developer Panel feature summary in `Home` so it reflects SMS operations in addition to magic-link auth, analytics, and email
- Result: the remaining AI2 planning docs are no longer floating as isolated files; they are now surfaced through the main docs index the same way the registration and theme plans already were

### Developer Panel SMS Aligned With Current Launch Model
- Re-read the current developer docs and code after AI Verdent's developer-panel pass to verify what was actually implemented vs what still reflected the older SMS architecture
- `app/developer/sms/page.tsx` was rewritten so the platform owner now sees the real launch SMS model:
  - toll-free-first for new workspaces
  - email-only fallback when no sender is configured yet
  - grandfathered manual / 10DLC cases kept visible as legacy states
  - `Element Barbershop` shown as the protected legacy/manual review case that must not be auto-migrated
- `backend/index.js` developer routes were extended with:
  - `GET /api/vurium-dev/sms/status`
  - `POST /api/vurium-dev/sms/provision`
  - `GET /api/vurium-dev/platform` now also returns `sms_number_type`
- `app/developer/_lib/dev-fetch.ts` now throws on non-2xx responses instead of blindly returning JSON, so developer actions like SMS provisioning no longer show false success toasts on `409` / `500`
- Toll-free provisioning logic is now shared instead of being duplicated:
  - owner path `POST /api/sms/enable-tollfree`
  - superadmin path `POST /api/vurium-dev/sms/provision`
- Toll-free provisioning failure now marks the workspace as `failed` instead of leaving it silently stuck in `provisioning`
- `docs/Features/Developer Panel.md` and `docs/Features/Developer Panel — Improvement Plan.md` were synced so they no longer describe:
  - Gmail as merely "planned"
  - developer SMS as frontend-only
  - the old platform-level "one campaign / one toll-free number" mental model

### SMS Finalization Plan Added
- Added `docs/Tasks/SMS Finalization Plan.md` as the explicit execution plan for finishing SMS
- Split responsibilities clearly between:
  - AI 2: frontend UX, consent text, legal alignment, live reviewer-facing verification
  - AI 1: Telnyx Verify secret, OTP verification, sender-path validation, Element protection, and launch-gate ops work
- Linked the plan from `docs/Tasks/In Progress.md` so both AIs can work from the same SMS checklist
- Added one explicit backend verification item too: AI 1 now needs to confirm whether `POST /api/sms/enable-tollfree` should really mark the workspace `active` immediately, or whether the status lifecycle needs one more “configured / pending delivery” step before launch

### SMS UX Reframed Around Auto-Activation
- Re-read the hardening plan and SMS finalization docs before changing the frontend wording so the UI matched the new launch consensus instead of the older manual-enable path
- `app/settings/page.tsx` now presents toll-free SMS as something that usually starts automatically after trial or paid-plan activation
- The Settings action is now positioned as:
  - automatic setup state
  - automatic retry state if an earlier attempt failed
  - status review once the workspace sender is configured
- `app/signup/page.tsx` SMS step was also reframed so it no longer sounds like owners are expected to manually enable reminders as a normal setup step
- After the owner confirmed "everything should work automatically from the first minute", the remaining manual CTA was removed from both Settings and signup for the default new-workspace path
- `app/settings/page.tsx` now also understands the backend auto-provision lifecycle better:
  - scheduled background retries still read as automatic retry
  - terminal `failed_max_retries` is shown as a support-review-needed state instead of pretending it is still retrying forever
- Follow-up doc cleanup: `docs/Tasks/Launch Readiness Plan.md`, `docs/Tasks/SMS Finalization Plan.md`, and `docs/Tasks/In Progress.md` were updated so the verification steps no longer tell reviewers to manually "enable SMS" on fresh workspaces
- Result: the frontend now matches the documented launch decision more closely: keep the dual-path architecture, but remove the obvious owner friction wherever possible

### Obsidian Documentation Rules Formalized
- Added an explicit `Documentation Rules (Obsidian)` section to `docs/AI-Work-Split.md`
- This now tells all AI to:
  - re-read docs before changing code or plans
  - always update `DevLog`, `In Progress`, and the relevant feature / task docs
  - use Obsidian wiki links for internal references
  - avoid duplicate docs and orphaned notes
  - record ownership, blockers, and exact status clearly enough that the repo can be understood directly from Obsidian

### GitHub Secrets Snapshot Recorded
- Owner provided a repository-secrets snapshot from the GitHub UI on 2026-04-14
- Added `docs/Architecture/GitHub Secrets Inventory.md` as the owner-confirmed Obsidian note for which secret names are actually visible in the repo
- Important Telnyx consequence:
  - `TELNYX_API_KEY` — present
  - `TELNYX_FROM` — present
  - `TELNYX_WEBHOOK_PUBLIC_KEY` — present
  - `TELNYX_VERIFY_PROFILE_ID` — not visible in that snapshot, so docs now treat it as unconfirmed / likely missing until rechecked
- Updated `docs/Architecture/Infrastructure.md` and `docs/Tasks/Telnyx-Integration-Plan.md` so they match the owner-confirmed secrets snapshot instead of the older assumption that the webhook public key still needed to be created

### SMS Delivery Research Note Added
- Added `docs/Tasks/SMS Delivery Options Research.md` as a reference memo for broader SMS architecture decisions beyond the current Telnyx launch path
- Captures:
  - REST vs SMPP vs SOAP vs SMTP delivery options
  - CPaaS vs local aggregators vs direct operator connections
  - sender strategy tradeoffs: alpha sender ID, long code, short code, toll-free
  - Ukraine-specific constraints and why country-aware messaging rules matter
  - recommended messaging architecture by product stage: small / medium / enterprise
- This is intentionally a research reference, not the active launch decision doc

### Privacy / Terms Alignment for SMS Review
- `app/privacy/page.tsx` and `app/terms/page.tsx` were updated so the legal pages match the real booking consent copy and the current SMS product direction
- Appointment notifications are now described as a business-specific program (`[Business Name] Appointment Notifications`) instead of a single hard-coded `VuriumBook Appointment Notifications` program
- The legal pages now explicitly reflect the dual-path sender model:
  - new workspaces default to dedicated toll-free reminder senders
  - grandfathered manual workspaces keep their registered dedicated sender path
- Payment language was broadened from Stripe-only wording to Stripe / Square / Apple where applicable
- Terms no longer advertise a `99.9% uptime` target or a `status.vurium.com` page that was not part of the confirmed launch setup

### SMS Pivot — Toll-Free Default + Grandfathered Manual 10DLC
- Product direction changed from "manual 10DLC as the default launch path" to a **dual-path** model:
  - new workspaces default to dedicated toll-free reminder setup
  - existing / pending 10DLC workspaces stay on the current manual path
  - OTP remains on the public Telnyx Verify routes
- `Element Barbershop` is explicitly preserved as a protected failed-review manual 10DLC case and should not be migrated during the toll-free pivot
- `app/settings/page.tsx` now shows a **toll-free-first** card for new workspaces and hides the SP / EIN-heavy flow behind a manual fallback
- Grandfathered workspaces keep the existing SP / 10DLC UI and are clearly labeled as the legacy / manual path
- `app/signup/page.tsx` no longer frames business registration / EIN collection as the default SMS reminder setup for new customers
- `backend/index.js` now tags manual business registrations with `sms_number_type: '10dlc'`
- `backend/index.js` fallback `sms_consent_text` now matches the post-pivot appointment-notifications wording too, so fallback records do not store the older pre-pivot text
- `app/settings/page.tsx` and `app/signup/page.tsx` toll-free copy were softened too, so the product no longer over-promises that reminder delivery is already fully live before the remaining Telnyx / pilot sign-off work is complete
- The user-facing toll-free state is now documented as `Configured` even though the backend currently writes `active`, so docs and UI use the same language while AI 1 verifies the final status lifecycle
- `app/settings/page.tsx` SMS actions now use the shared auth-aware API helper instead of raw `fetch(window.__API...)`, so toll-free enable, manual registration, and OTP verification follow the same Bearer-token / stale-session behavior as the rest of Settings
- `app/settings/page.tsx` mobile `Roles & Permissions` now expands/collapses per page row (`Dashboard`, `Calendar`, `History`, `Clients`, etc.) instead of forcing one very long always-open list on phone screens
- Appointment messaging no longer falls back to the platform global sender when a workspace does not have active SMS; the product-safe fallback is now **email-only**
- We also documented one remaining launch-risk detail: the backend still writes `sms_registration_status: 'active'` immediately after toll-free provisioning, so AI 1 must confirm that this matches real delivery readiness or tighten the lifecycle semantics before sign-off
- `docs/Tasks/SMS-Strategy-Review.md` was rewritten into a dual-path memo:
  - Square officially documented as toll-free
  - Booksy officially documented as centrally managed, but sender rail left as unproven externally
  - Element remains the live manual-path example
- Added an explicit 3-AI SMS execution split to `docs/Tasks/SMS Finalization Plan.md` and `docs/Tasks/In Progress.md`:
  - `Claude / AI 1` = backend hardening and SMS docs
  - `Codex / AI 2` = frontend SMS auto-activation UX in `app/settings/page.tsx` and `app/signup/page.tsx`
  - `Verdent` = reviewer / verification / research support, not a second backend owner
  - `Owner` = Telnyx secrets and Jonathan follow-up

### Planning Docs Revised Into Launch-Closure State
- `docs/Tasks/Launch Readiness Plan.md` was rewritten from a broad discovery backlog into a stable launch-closure plan
- `docs/Tasks/SMS-Strategy-Review.md` was rewritten into a launch decision memo instead of a 4-option pre-launch architecture debate
- Current launch path is now explicitly documented as:
  - Telnyx Verify for OTP / signup / booking verification
  - toll-free-first reminder setup for new workspaces
  - grandfathered manual 10DLC setup for existing / pending workspaces
- Non-launch-default SMS architecture debates were moved out of the critical path:
  - forced legacy migration
  - ISV single-sender retry path
  - other post-pilot messaging experiments
- The planning doc roles are now clearer:
  - `Launch Readiness Plan` = stable decision doc
  - `In Progress` = operational tracker
  - `DevLog` = history
  - `SMS Strategy Review` = messaging decision memo only
- `TELNYX_VERIFY_PROFILE_ID` is now documented as an operational dependency, not a missing product architecture task

### Smart Booking Audit System (`runBookingAudit()`)
Background job running every 4 hours, scans all workspaces for booking anomalies.

**8 Health Checks:**

| # | Check | Severity | Action |
|---|-------|----------|--------|
| 1 | Double bookings (overlapping times, same barber) | critical | Alert |
| 2 | Ghost barber (deleted/inactive barber on future booking) | critical | SMS + email to client |
| 3 | Stale "booked" status (past >2h) | warning | **Auto-fix to noshow** |
| 4 | Missing client data (no name, phone, or email) | warning | Alert |
| 5 | Orphaned bookings (expired workspace plan) | critical | Alert |
| 6 | Schedule violations (booking outside working hours) | warning | Alert |
| 7 | Cancellation spike (>50% in 24h, min 5 bookings) | warning | Alert |
| 8 | Repeat no-show clients (3+ in 30 days) | info | Alert |

**Notifications:**
- `support@vurium.com` — digest of all critical issues across all workspaces
- Workspace owner — push notification + branded dark-cosmos email
- In-app badge — stored in `settings/booking_audit` doc per workspace

### Auto-Fix Stale Bookings
- Past bookings with `status: 'booked'` (>2 hours overdue) automatically changed to `noshow`
- Adds `auto_noshow: true` + `auto_noshow_at` timestamp for audit trail
- Previously only warned; now self-heals

### Ghost Barber Client Alerts
When audit detects a future booking assigned to a deleted/inactive barber:
- **SMS** to client: "Your specialist is no longer available... rebook here"
- **Email** to client: branded template with "Reschedule" + "Book Another Specialist" buttons
- Dedup via `ghost_barber_notified` flag on booking doc — won't spam on next audit cycle

### Smart Client Recommendations (`getSmartRecommendation()`)
When a public booking fails, the API now returns actionable alternatives:
- On `409 Conflict` — next 3 available slots for same barber + up to 3 alternative barbers
- On `400 Outside Schedule` — next available slots within working hours
- On `404 Barber Not Found` — other active barbers with their nearest open slot

Response includes `recommendation` object:
```json
{
  "error": "Slot already booked",
  "recommendation": {
    "message": "This time is unavailable. Here are the nearest open slots:",
    "alternative_slots": ["2026-04-15T15:00:00Z", "..."],
    "alternative_barbers": [{ "id": "abc", "name": "Mike", "next_slot": "..." }]
  }
}
```

Enhanced endpoints:
- `POST /public/bookings/:workspace_id` (conflict, outside schedule, barber not found)
- `POST /public/manage-booking/reschedule` (conflict, outside schedule)

### Client Satisfaction Ping + Google Reviews
After a booking is marked `done`/`completed`:
- **Email immediately** — branded "How was your visit?" with star rating icons + "Leave a Google Review" button
- **SMS 2 hours later** — "Thanks for your visit! Leave a review here: {google_review_url}"
- Dedup: won't schedule if satisfaction reminder already exists for that booking
- Respects SMS opt-out via existing `runAutoReminders()` pipeline

**New settings:**
- `google_review_url` — owner saves their Google Review link
- `satisfaction_sms_enabled` — toggle on/off (defaults to on)

### Waitlist Auto-Fill
When any booking is cancelled (3 paths: PATCH status, DELETE, public cancel):
1. Checks if the cancelled slot is in the future and actually free
2. Queries waitlist for matching `barber_id` + `date` + preferred time window
3. Sends first matching waitlist client SMS + branded email: "A spot just opened up!"
4. Marks waitlist entry as `notified: true, auto_fill: true`
- Reuses existing waitlist notification email template (party icon + table + "Book Now" CTA)

### Booking Rate Limiter
- In-memory sliding window: max 3 bookings per phone/email per hour per workspace
- Returns `429 Too many booking requests`
- Applied to `POST /public/bookings/:workspace_id` and `POST /public/bookings-group/:workspace_id`
- Records hit only after successful booking (not on validation errors)
- Auto-cleanup of expired entries every 10 minutes

### Booking Audit Status Endpoint
- `GET /api/booking-audit/status` — returns last audit results from `settings/booking_audit`
- Fields: `last_run`, `warnings_count`, `critical_count`, `warnings[]`
- Pattern matches existing `GET /api/payroll/audit/status`

## Files Changed
- `backend/index.js` — all features: `runBookingAudit()`, `getSmartRecommendation()`, `scheduleSatisfactionPing()`, `tryWaitlistAutoFill()`, `checkBookingRateLimit()`, `recordBookingRateHit()`, enhanced error responses, booking audit status endpoint, google_review_url + satisfaction_sms_enabled settings

## Commits
- `c24dbe8` — smart booking audit + self-scanning + AI recommendations
- `7846902` — client satisfaction ping + Google review link
- `45d787d` — auto-fix stale bookings, ghost barber alerts, waitlist auto-fill, rate limiter

---

## AI coordination update — AI 4 GitHub review sync rule

Added a cross-AI process rule so Phone AI / AI 4 reviews that land in GitHub are not effectively invisible to the rest of the team.

### What changed
- Added an active rule in [[AI-Rule-Updates]]: GitHub-side AI 4 reviews must be synced into their real local `docs/Tasks/*.md` path
- Updated [[AI-Core-Manifesto]] so GitHub-only AI 4 reviews do **not** satisfy the 4-AI Plan Review Gate
- Updated [[AI-Profiles/AI-4-Phone-AI|AI 4 profile]] with an explicit requirement to sync the real review doc locally
- Updated [[Home]], [[Tasks/In Progress|In Progress]], and [[Tasks/3-AI-Remaining-Work-Split|4-AI Remaining Work Split]] to surface the rule at startup
- Explicitly avoided a duplicate mirror-file approach; the real review doc is the source of truth once synced locally

### Why
- AI 4 often works from phone/mobile and can leave reviews in GitHub before those findings appear in local docs
- That created a visibility gap: some AIs could miss AI 4 feedback entirely
- From now on, the team treats AI 4 review as complete only after the real review doc exists in the local repo too

---

## AI 4 review synced locally

Synced AI 4's GitHub-side docs review into the local repo at its final path so the full team can see it without relying on GitHub visibility.

### Added
- [[Tasks/AI4-Emergency-Readiness-Review-2026-04-15]] — local copy of AI 4's emergency-readiness review

### Updated
- [[Tasks/In Progress]] 4-AI Plan Review Gate for both BE.8 and BE.9 now marks AI 4 review as landed and locally available

### Review result
- AI 4 signoff is effectively green from an emergency-response lens
- 5 follow-up asks were recorded:
  - `AI4-REQ.1` job_locks emergency runbook
  - `AI4-REQ.2` last-known-good SHA convention
  - `AI4-REQ.3` AI 4 activation protocol
  - `AI4-REQ.4` `requireSuperadmin` endpoint inventory
  - `AI4-REQ.5` secret rotation protocol documentation
