# QA Scan — 2026-04-15

> [[Home]] > QA & Quality | See also: [[QA-Scanner-Guide]], [[Tasks/QA-Scan-2026-04-14|QA Scan 2026-04-14]]
>
> Scanner: Verdent (VR.6)
> Files scanned: `backend/index.js`, `components/Shell.tsx`, `app/settings/page.tsx`
> Scope: Status check on open bugs from 2026-04-13/14 scans + new issues from Sprint 1 changes

---

## Previously reported bugs — status update

### ✅ FIXED since last scan

| Bug ID | Description | Fixed by | Commit |
|---|---|---|---|
| BUG-012 | `runAutoReminders` capped at 100 workspaces | Gap 4 — `while/startAfter(50)` pagination | `3efce7e` |
| BUG-015 | Settings SMS forms used raw `fetch()` without auth token | `apiFetch()` / `apiRequest()` in `app/settings/page.tsx` | `3efce7e` |
| Gap 3 | Telnyx STOP webhook O(N) workspace scan | `phone_number_index` O(1) + `collectionGroup('clients')` | `3efce7e` |
| BUG-013 | Stripe webhook after auth middleware → 401 | `isWebhookEndpoint()` helper bypasses auth+CSRF for webhooks | `849e998` |
| BUG-002 | `/api/push/status` unauthenticated info disclosure | Gated behind `requireSuperadmin` | `849e998` |
| BUG-014 | Push token dedup used `req.workspaceId` (undefined) | Changed to `req.wsId` | `849e998` |
| NEW-002 | Apple IAP auto-provision used `req.workspaceId` (undefined) | Changed to `req.wsId` | `849e998` |
| SET-001–007 | `GET /public/config` missing 7 booking/display fields | Explicit allowlist: `display`, `booking`, `online_booking_enabled`, `waitlist_enabled`, `tax/fees/charges` | `849e998` |
| NEW-001 | `SMS & 10DLC.md` stale "Auto-provision Removed" row | Updated to reflect `autoProvisionSmsOnActivation()` | `849e998` |
| BE.4 | `PATCH /api/messages/:id/reactions` missing → 404 | Implemented with `arrayUnion/arrayRemove`, emoji allowlist | ⏳ uncommitted |
| **BE.1** | **Distributed lock for background jobs** | **`withJobLock()` helper, 7 jobs wrapped** | **`5dab7a1`** ✅ |

---

## Post-Commit Review Notes (AI 3)

### BE.1 withJobLock — APPROVED with note
- **Review doc:** [[Tasks/BE.1-Post-Commit-Review]]
- **Status:** Code correct, transactions valid, all 7 jobs wrapped
- **Note:** Firestore TTL not automatic — `job_locks/` docs persist until released or manual cleanup
- **Action:** Owner to verify Cloud Run logs show `[JOBS] Instance id: ...`

---

### ❌ STILL OPEN — not fixed

| Bug ID | Severity | Description | Fix owner | File / Line |
|---|---|---|---|---|
| BUG-002 | HIGH | `/api/push/status` registered at line 1089, **before** auth middleware at line 3323 — unauthenticated, exposes APNs metadata | AI 1 | `backend/index.js:1089` |
| BUG-003 | HIGH | `_wsPushPrefsCache` is a global singleton (not workspace-keyed) — 30s cross-tenant cache leak | AI 1 | `backend/index.js:999` |
| BUG-007 | MEDIUM | ProfileModal password tab unreachable — `(['profile'] as const).map(...)` renders only one tab button | AI 2 | `components/Shell.tsx:309` |
| BUG-008 | MEDIUM | GDPR data export: `req.userId` (undefined) — should be `req.user.uid` | AI 1 | `backend/index.js:10770` |
| BUG-013 | HIGH | Stripe billing webhook at line 10384, **after** auth middleware at line 3323 → webhooks rejected with 401 | AI 1 | `backend/index.js:10384` |
| BUG-014 | MEDIUM | Push token dedup: `req.workspaceId` undefined (should be `req.wsId`) at line 8155 — skips nothing | AI 1 | `backend/index.js:8155` |
| SET-001–007 | HIGH | `GET /public/config` returns only 9 fields — 7 booking/display settings never reach public booking page | AI 1 | `backend/index.js:8931` |
| PERM-001–005 | HIGH | Permissions system: `visibleNav` not rendered, dashboard hardcoded `isBarber`, backend ignores `role_permissions` | AI 2 / AI 1 | `Shell.tsx:309`, `dashboard/page.tsx:668` |
| BUG-004 | LOW | `Shell.tsx` profile modal: `newPw.length < 4` — but password tab unreachable so no user impact right now | AI 2 | `components/Shell.tsx` |
| BE.4 | MEDIUM | `PATCH /api/messages/:id/reactions` endpoint missing — returns 404 in production | AI 1 | `backend/index.js` — not implemented |

---

## New items from Sprint 1 changes

### [NEW-001] `SMS & 10DLC.md` stale row — Auto-provision incorrectly marked "Removed"
- **File**: `docs/Features/SMS & 10DLC.md` line 386
- **What**: Row says `Auto-provision TFN on signup | ❌ Removed — SMS via Settings now`
- **Reality**: `autoProvisionSmsOnActivation()` is implemented at `backend/index.js:2099` and called from signup (line 3821), Apple verify (line 10372), and Stripe events (line 10466)
- **Impact**: Misleads AI agents about implementation state — causes duplicate work risk
- **Fix owner**: AI 1 (docs scope)
- **Status**: NEW — fix in next docs commit

### [NEW-002] `req.workspaceId` used in apple-verify for auto-provision
- **File**: `backend/index.js` line 10372
- **What**: `autoProvisionSmsOnActivation({ id: req.workspaceId, ... }, 'apple_paid')` — but push register uses `req.wsId` (the correct middleware field). Need to verify which field apple-verify middleware sets.
- **Impact**: If `req.workspaceId` is undefined here, auto-provision for Apple IAP path silently fails — workspace ID would be empty string
- **Check**: Confirm `resolveWorkspace` middleware sets `req.workspaceId` OR `req.wsId` for the `/api/billing/apple-verify` route
- **Fix owner**: AI 1
- **Status**: NEW — needs verification

### [NEW-003] `runSmsAutoProvisionRetry` iterates ALL workspaces every 5 min
- **File**: `backend/index.js` lines 9451–9499
- **What**: Every 5-minute cycle paginates all workspaces looking for `sms_auto_provision_next_retry_at`. At scale this reads all workspace `settings/config` docs. Currently capped at `maxPerCycle = 500`.
- **Impact**: At 500+ workspaces: 500 Firestore reads every 5 minutes just for retry checks. Not a launch blocker but a future cost concern.
- **Suggested optimization**: Add a top-level `sms_provision_needs_retry: true` flag per workspace, query only those — O(flagged workspaces) not O(all workspaces).
- **Fix owner**: AI 1 (post-launch P3)
- **Status**: NEW — not a launch blocker

---

## Gap 2 — Webhook signature verification status

| Condition | Status |
|---|---|
| `verifyTelnyxWebhookSignature()` function in code | ✅ Present at `backend/index.js:1721` |
| Called in `POST /api/webhooks/telnyx` | ✅ Line 1749 |
| Called in `POST /api/webhooks/telnyx-10dlc` | ✅ Line 1818 |
| `TELNYX_WEBHOOK_PUBLIC_KEY` in `deploy-backend.yml` | ✅ Line 64 |
| `TELNYX_WEBHOOK_PUBLIC_KEY` in GitHub Secrets | ⏳ **Owner action required** |
| Enforcement active in production | ⏳ Pending secret — currently soft no-op |

---

## Launch blockers — поточний стан

| # | Item | Owner | Статус |
|---|---|---|---|
| 1 | BUG-013: Stripe webhook після auth → 401 | AI 1 | ✅ Fixed `849e998` |
| 2 | BUG-002: `/api/push/status` без auth | AI 1 | ✅ Fixed `849e998` |
| 3 | SET-001–007: Public config без booking/display полів | AI 1 | ✅ Fixed `849e998` |
| 4 | PERM-001: `visibleNav` не рендериться | AI 2 | ❌ Open |
| 5 | NEW-002: `req.workspaceId` в apple-verify | AI 1 | ✅ Fixed `849e998` |
| 6 | `TELNYX_WEBHOOK_PUBLIC_KEY` secret | Owner | ⏳ Очікує |
| 7 | NEW-001: `SMS & 10DLC.md` стейл рядок | AI 1 | ✅ Fixed `849e998` |
| 8 | BE.4: reactions endpoint 404 | AI 1 | ⏳ Uncommitted |
| 9 | BUG-003: `_wsPushPrefsCache` cross-tenant | AI 1 | ❌ Open |
| 10 | BUG-008: GDPR `req.userId` → `req.user.uid` | AI 1 | ❌ Open |

---

## Passed checks

- `runAutoReminders` pagination: ✅ `while/startAfter(50)`, no `limit(100)`
- `verifyTelnyxWebhookSignature`: ✅ Ed25519 helper present and called in both handlers
- `phone_number_index` write: ✅ Present in `provisionTollFreeSmsForWorkspace` at line 2039
- `phone_number_index` read: ✅ O(1) lookup at line 1768 in telnyx webhook
- `collectionGroup('clients')` for STOP: ✅ Line 1779
- `autoProvisionSmsOnActivation`: ✅ Present at line 2099, called from 3 activation paths
- `deploy-backend.yml` env vars: ✅ Both `TELNYX_VERIFY_PROFILE_ID` and `TELNYX_WEBHOOK_PUBLIC_KEY` wired
- Password min-8 in `app/settings/page.tsx`: ✅ Line 448 enforces `< 8`
- OTP rate limit (3/phone/10min): ✅ Still in place
- Element Barbershop legacy guard: ✅ `isProtectedLegacyWorkspace` still intact

---

## Retrospective: Sprint 1 Regressions (for future scans)

### Waitlist regression (2026-04-15) — SET-006 fix caused CTA disappearance

**What happened:**
- Commit `849e998` (SET-006 fix) forced `waitlist_enabled: false` in `/public/config/` for every workspace that never touched the Settings toggle
- Frontend `??` fallback couldn't kick in because `false` is not nullish
- Result: `Join waitlist` CTA disappeared from public booking page

**Root cause:**
- Backend wrote explicit `false` instead of leaving field undefined
- Frontend relied on nullish coalescing for default-on behavior

**Fix:**
- Commit `a3c885f` — changed to conditional spread so undefined falls through to plan feature check
- Semantic: waitlist is ON by default on any plan that includes it; Settings toggle is explicit OFF override

**Lesson:**
- When changing default behavior, check both backend write sites AND frontend fallback logic
- Explicit `false` in Firestore blocks frontend `??` — use `undefined` or omit field for "use default"

**Pattern to watch for in future scans:**
- Any `field: false` write that replaces `field: undefined` or field omission
- Any boolean field where `false` should mean "default" rather than "explicitly off"
