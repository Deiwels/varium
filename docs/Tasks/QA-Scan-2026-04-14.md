# QA Scan — 2026-04-14 (AI 1 review of unstaged changes)

> [[Home]] > QA & Quality | See also: [[QA-Scanner-Guide]], [[Tasks/QA-Scan-2026-04-13|QA Scan 2026-04-13]]

> Scanner: AI 1
> Files scanned: `backend/index.js` (unstaged SMS/Verify changes by owner)

## POTENTIAL ISSUE

### [SMS-001] Verify endpoints must stay public for signup/booking — RESOLVED
- **File**: `backend/index.js` — active routes are `POST /public/verify/send/:workspace_id` and `POST /public/verify/check/:workspace_id`
- **What**: Signup already calls the public workspace-scoped routes, so OTP verification must remain usable before login.
- **Resolution**: The public route contract has been restored and now calls Telnyx Verify when `TELNYX_VERIFY_PROFILE_ID` is configured, with a legacy local-code fallback until that env var exists.
- **Fix owner**: AI 1 / Owner
- **Status**: DONE

### [SMS-002] Duplicate messageFlow and optout fields in SP registration
- **File**: `backend/index.js` — SP campaign registration
- **What**: The diff shows `messageFlow`, `optoutKeywords`, `optinMessage`, `optoutMessage` defined twice — once for standard campaign and once for SP campaign. Both blocks have identical content.
- **Impact**: Not a bug, but code duplication. If one is updated and the other forgotten, they'll diverge.
- **Fix**: Extract shared campaign fields into a helper function
- **Fix owner**: AI 1
- **Status**: LOW — cosmetic

## PASSED

- New Verify endpoints have proper rate limiting (3 per phone per 10 min) ✓
- Telnyx API calls have try/catch with meaningful error messages ✓
- Phone number sanitized with formatPhone + safeStr ✓
- Code validation checks length >= 4 ✓
- Auto-TFN removal is clean — replaced with comment explaining new flow ✓
- SP registration status set to 'active' (SP campaigns auto-approve) ✓
- No syntax errors in new code ✓
