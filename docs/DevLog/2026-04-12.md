# 2026-04-12

## Done

### Payroll — full restyling (Varium dark cosmos)
- Single-column layout with KPI strip (5 cards: services, team, owner, tips, total)
- Full-width table, tighter padding
- Bottom panels: owner profit + admin left, tabs right
- Commission Rules opens in modal
- Glass blur topbar, nebula accents, deeper card shadows

### Payroll — admin profit proportional to days
- Was: full profit share regardless of attendance
- Now: `profitShare = ownerShare * pct * (daysWorked / periodDays)`
- Shows "Profit 2% (3/7d): $X.XX"

### Tips consistency fix
- POST handler saved only `tip_amount`, PATCH sent only `tip`
- Now both fields saved together in both handlers

### Cash register — not counting
- Field names mismatched: backend `payment_method`/`amount_cents` vs frontend `method`/`amount`
- Fixed to handle both naming conventions

### Cash total inflated
- `/api/payments` used `amount_cents` (total with tax) instead of `service_amount` (net)
- `tip_amount` (dollars) was divided by 100 as if cents
- Fixed: local payments use `service_amount`, tips as dollars

### Cash page — removed duplicated cards
- Day rows show compact inline amounts, no repeated KPI boxes

### Expenses system
- `/api/expenses` now filters by date params (was ignoring them)
- `/api/expenses/total` returns `by_category` breakdown
- Frontend handles array response
- Payroll shows expense category breakdown

### Payroll Smart Audit
- `GET /api/payroll/audit` — 7 checks:
  1. Unpaid completed bookings (skips blocks, pending, $0)
  2. Booking-Payment match
  3. Cash reconciliation
  4. Admin attendance
  5. Period totals
  6. Missing service amounts
  7. Square verification (linked payments only, unlinked shown separately)
- "Audit" button in payroll topbar with expandable results panel
- Color-coded: green ok, gold warning, red error

### Automatic audit notifications
- Background job every 6 hours (throttle in Firestore, survives cold starts)
- Anti-spam: hash comparison, only notifies on new issues
- Push to owner only (APNs)
- Email to owner only (Resend, no emojis)
- Red badge with ! on Payroll icon in Dashboard

### Audit accuracy fixes
- Unpaid check: filters out blocks, pending, booked, $0 bookings
- Square check: compares only linked payments (matching payment_id)
- Unlinked Square payments shown as info, not mismatch

### Onboarding Wizard
- 4-step wizard replaces empty dashboard for new workspaces
- Step 1: Business info (name, type pill selector, address) — auto-skips if filled during signup
- Step 2: Add first staff member (label adapts: Barber/Stylist/Artist via terminology.ts)
- Step 3: Select services from business-type templates (editable price/duration, custom add)
- Step 4: Enable online booking + iframe preview of booking page
- Detection: shows when `barbers.length === 0 && services.length === 0 && isOwnerOrAdmin`
- Dismissable via Skip → localStorage `VB_ONBOARDING_DISMISSED`
- On complete → dashboard reloads with real data

### Business Templates
- 7 business types with pre-filled services:
  - Barbershop (6): Haircut, Beard Trim, Haircut+Beard, Kids, Hot Towel, Lineup
  - Hair Salon (6): Women's/Men's Haircut, Blowout, Color, Highlights, Deep Conditioning
  - Nail Studio (6): Classic/Gel Manicure, Classic/Gel Pedicure, Nail Art, Polish Change
  - Beauty Salon (5): Facial, Waxing, Makeup, Threading
  - Spa & Wellness (5): Swedish/Deep Tissue/Hot Stone Massage, Body Scrub, Aromatherapy
  - Tattoo Studio (6): Small/Medium/Large Tattoo, Consultation, Touch-Up, Piercing
  - Lash & Brow Bar (6): Classic/Volume Lash Extensions, Fill, Brow Lamination, Tint, Lift

### Backend: online_booking_enabled
- Added `online_booking_enabled` to ALLOWED_SETTINGS in POST /api/settings

### Obsidian Documentation Vault
- Created `docs/` folder with full project documentation
- Architecture: Tech Stack, Database Schema, Auth Flow
- Features: Booking System, Payments, Client Management, AI Style, Attendance & Payroll
- Backend: API Routes (193 endpoints), Firestore Collections (23)
- Frontend: App Routes (43 pages), Components (10)
- DevLog + Tasks tracking
- Added `.obsidian/` to `.gitignore`

## Files changed
- `app/payroll/page.tsx` — restyling, audit UI, formulas
- `app/cash/page.tsx` — field normalization, compact layout
- `app/expenses/page.tsx` — response parsing
- `app/calendar/booking-modal.tsx` — tips consistency
- `app/dashboard/page.tsx` — audit badge, onboarding wizard integration, services fetch
- `backend/index.js` — audit endpoint, periodic job, tips fix, expenses fix, cash normalization, online_booking_enabled
- `components/Shell.tsx` — removed audit badge from bottom nav
- `components/OnboardingWizard.tsx` — **NEW** — 4-step setup wizard
- `lib/onboarding-templates.ts` — **NEW** — business type service templates
