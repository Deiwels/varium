# 2026-04-14

> [[Home]] > DevLog | Related: [[Developer Panel]], [[Production-Plan-AI1]], [[Production-Plan-AI2]]

## Vurium Inc. — Corporation Approved
- Illinois Secretary of State approved Articles of Incorporation
- File Number: 75519095, Packet Number: 17758290522194867­4
- PDF available at apps.ilsos.gov/corparticles/status.jsp
- Annual report required before April each year + franchise tax

## 10DLC Brand — Vurium Inc. Registered on Telnyx
- New brand created: TCR ID: BCFAC3G, Brand ID: 4b20019d-7837-173f-8055-44b9fffbc815
- Status: Unverified — CP-575A + Articles of Incorporation need to be sent to 10dlcquestions@telnyx.com
- New EIN may take up to 2 weeks to propagate in IRS TIN matching
- Campaign creation blocked until brand is Verified (only 2FA and Machine to Machine use cases available for unverified brands, need CUSTOMER_CARE)

## Telnyx Support — Jonathan's Response
- Confirmed: per-customer brand/campaign is REQUIRED (shared campaign NOT an option)
- Sole Prop brands: OTP verification, 2.5 business days approval
- Demo campaign available — can move customers to regular campaign once spun up
- Offered call next week
- Replied with: Vurium Inc. incorporation update, Element Barbershop "on behalf of" fix request, 2FA campaign fix, call availability Mon-Fri 10AM-4PM CT

## Telnyx Campaign Status — 2FA TELNYX_FAILED
- Brand: Vurium (old Element Barbershop Co brand), TCR: C1BWNV7
- Rejected because: campaign registered as 2FA but Privacy Policy described appointment reminders, not 2FA; "system notifications" too vague; company name mismatch (Element Barbershop Co vs Vurium Inc.)
- Fix: Privacy Policy already has Program A (Appointments) + Program B (2FA). Will resubmit under Vurium Inc. brand once verified.

## Legal Pages — Fixes
- Privacy Policy: address format fixed (ZIP was on street line instead of city line)
- Terms of Service: section numbering fixed (duplicate section "9." → now 9. Withdrawal, 10. Limitation of Liability)

## Developer Panel — New Feature (`/developer`)
Private admin panel for vurium.com with separate auth system.

### Auth — Magic Link
- Separate from VuriumBook auth (no VuriumBook account needed)
- Enter email on `/developer/login` → receive magic link via Resend → click → HttpOnly cookie session (24h)
- Rate limited: 3 requests per 15 min per IP
- One-time use tokens (jti + Set)
- IP logging on every login (Firestore `vurium_dev_logins`)
- Cookie: `vurium_admin_token`, HttpOnly, Secure, SameSite=none (for cross-origin vurium.com ↔ run.app)
- Env var: `ADMIN_EMAIL` controls who receives magic link

### Overview Page (`/developer`)
- Workspace count (total, paid, trialing)
- Trial conversion rate
- Signups (7d, 30d) + signup chart
- Plan breakdown (Individual / Salon / Custom)
- Platform totals: bookings, clients, staff across all workspaces
- Workspaces table: name, slug, plan, billing status, bookings, clients, staff, SMS status, created date
- Backend: `GET /api/vurium-dev/platform` aggregates from all Firestore workspace docs

### Analytics Page (`/developer/analytics`)
- Custom privacy-friendly tracker (`public/va.js`, ~1KB)
- Tracks: page URL, referrer, screen size, device type, browser
- Visitor ID via localStorage (not cookie), session ID via sessionStorage
- IP hashed with SHA-256 before storage
- SPA-aware: hooks into pushState/replaceState/popstate
- Backend: `POST /t` (unauthenticated, rate-limited 60/min per IP)
- Dashboard: pageviews by day (SVG chart), unique visitors, sessions, pages/session
- Top pages, top referrers, device breakdown, browser breakdown
- Conversion funnel: landing → signup → dashboard
- Range filter: 7d / 30d / 90d
- Firestore collection: `vurium_analytics`

### Email Page (`/developer/email`)
- Gmail mailbox quick links (support@, billing@, sales@, security@vurium.com)
- Branded compose: write text → sends via Resend with Vurium dark cosmos HTML template from noreply@vurium.com
- Sent history from Firestore `vurium_emails`
- Gmail API integration (frontend ready, backend endpoints needed — see next session)
- Inbound email forwarding: when email arrives, notification with preview sent to ADMIN_NOTIFY_EMAIL

### Backend Endpoints (all under `requireSuperadmin` middleware)
- `POST /api/vurium-dev/auth/request` — magic link request
- `POST /api/vurium-dev/auth/verify` — verify magic link → set cookie
- `POST /api/vurium-dev/auth/logout` — clear cookie
- `GET /api/vurium-dev/ping` — auth check
- `GET /api/vurium-dev/platform` — workspace/business metrics
- `GET /api/vurium-dev/analytics` — site analytics aggregation
- `POST /api/vurium-dev/email/send` — send branded email
- `POST /api/vurium-dev/email/inbound` — Resend inbound webhook
- `GET /api/vurium-dev/emails` — list emails
- `GET /api/vurium-dev/emails/:id` — get single email
- `PATCH /api/vurium-dev/emails/:id` — mark read/unread
- `POST /t` — analytics tracker ingest (unauthenticated)

### Files Created
- `public/va.js` — analytics tracker
- `app/developer/layout.tsx` — admin layout with SVG icon sidebar, mobile responsive
- `app/developer/page.tsx` — overview dashboard
- `app/developer/analytics/page.tsx` — site analytics
- `app/developer/email/page.tsx` — email inbox + compose
- `app/developer/email/[id]/page.tsx` — email detail
- `app/developer/login/page.tsx` — magic link login
- `app/developer/verify/page.tsx` — magic link verification

### Files Modified
- `backend/index.js` — all admin endpoints + tracker + platform metrics
- `middleware.ts` — `/developer` routes pass through (auth handled by layout)
- `app/layout.tsx` — added `<script src="/va.js" defer />`
- `.github/workflows/deploy-backend.yml` — added ADMIN_EMAIL, ADMIN_NOTIFY_EMAIL, FRONTEND_URL env vars

### CI/CD
- All 3 backend deploys passed (GitHub Actions → Cloud Run)
- Frontend auto-deployed via Vercel on git push

## Launch Readiness — AI 2 Frontend Progress
- Working in AI 2-owned files only: `app/settings/page.tsx`, `app/book/[id]/page.tsx`, and other client-facing frontend polish items from the launch plan
- `Settings` mobile UX now uses drill-down navigation on phone: tap category → dedicated detail screen → `All Settings` back button
- Public booking page now includes:
  - timezone label near time selection and again in the booking summary
  - booking form draft persistence for `name`, `email`, `phone`, `notes`, and SMS consent so data is not lost if user goes back or the selected slot changes
- `Sign in` no longer uses a native browser prompt for password reset; `Forgot password` now opens a branded modal with email input and styled success/error feedback
- Verified current frontend/backend settings path stays aligned for:
  - `online_booking_enabled`
  - `waitlist_enabled`
  - `booking.cancellation_hours`
  - `display.show_prices`
  - `display.require_phone`
  - `display.allow_notes`
- `Settings` and `Billing` owner flows no longer use native `confirm()` / raw `alert()` patterns in the edited screens; plan-management actions now go through the app's styled dialog flow
- Next AI 2 task in progress: continue customer-facing P0 polish in AI 2-owned screens without crossing into backend/shared ownership

## Next Steps
- [ ] Verify brand Vurium Inc. on Telnyx (send CP-575A to 10dlcquestions@telnyx.com)
- [ ] Reply to 10dlcquestions about deleted brand BQY3UXK
- [ ] Call with Jonathan (Telnyx) next week
- [ ] Add ADMIN_NOTIFY_EMAIL secret in GitHub for email forwarding
- [ ] Gmail API integration for Developer panel (OAuth setup needed)
- [ ] Wait for TFN +1-877-590-2138 verification
